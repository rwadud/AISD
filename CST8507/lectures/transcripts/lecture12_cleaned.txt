Before I talk about RAG, I need to touch base with what a large language model is and what the characteristics of different large language models are. Then we will talk about RAG. How does RAG work in a question-answer system? This technique you will use in your assignment.

So what is LM? Language model. What is the role of a language model? Generating text. Generate, predict the next word, the completion of your text.

But now, with a large language model, we also predict what's coming next. But here, it's in a massive amount. A massive amount in the training data, and also a massive amount of generated text. Using a large language model, we can generate a paragraph, we can generate a book, we can generate a huge amount of data. This is the basic difference between a language model and a large language model.

The other difference actually is architecture. Large language models are now built on transformers, a different architecture. A huge amount of parameters. This is the main difference. A large language model is a language model, but it's trained on a huge amount of data and with a different architecture technique, which is a transformer. And then the objective is to perform different NLP tasks. With a language model, the model just predicts the next word, the completion of your text. But using a large language model, I can perform different NLP tasks, not just generate text, as you will see. It will perform different NLP tasks.

I try to update this every semester. This is the best that I can find in 2024. I cannot find something in 2025. But as you can see, the development of large language models starting from 2019 is huge. It's not by months, not by year, it's by months. And even during the months, you may have a different version for the same model. This chart actually also reflects the amount of investment in this field, so many companies now invest in this field. That's why it's a huge stream here. We have some common techniques. ChatGPT is one well-known technique. Llama, DeepSeek, these are common techniques that we hear about now. This reflects how it has improved. This has happened in 2022, 2023, so it's a huge amount. It's divided by months from 7 to 10, 11 to 12, 4 to 6. A huge improvement and a huge investment in this field.

So any large language model can be identified with specific components. One component is the tokenization technique. Large language models use the parameters, the context length, and the architecture, which till now is a transformer. I read a paper a few days ago about a new architecture. Did you hear about this, I think called Mamba or something like that? They have a different architecture now. It's not used on a large scale yet, but they are thinking now about a different architecture. But for now, ChatGPT and all these models still work based on a specific architecture, which is the transformer. Maybe after a couple of months, we can find out there is a different architecture now. Mamba is one of the different architectures, and some research talks about a hybrid technique between the transformer and Mamba. So this is an open topic of research here now. But all of them are based on the transformer approach.

Now, the other components are tokenization, context window, and parameters. Look at this related to the parameters, a comparison between common large language models in terms of parameters. As you see here, it needs a huge number of parameters. What is a parameter? What's the definition of a parameter in any model? The weight. The weight that is initialized at the beginning. And during the learning process, this weight is adjusted according to your task.

So to understand or to imagine what this parameter is, for example, for a simple neural network: if you have a simple neural network, you have these three inputs, only two hidden layers. How many parameters are in this network? Can you count? It's fully connected, where each one is connected to the other one. So here, 3 multiplied by 4 plus 4, and here 4 multiplied by 4 plus 4, and here 4 multiplied by 1 plus 1. Very simple unit of work. How many parameters? About 40 or something like that. So imagine for a large language model, these numbers are in the billions. So this is the number of parameters that should be adjusted.

With an increased number of parameters, what do you mean by this? If the number of parameters increases in any large language model, what is your inference on this? What do we recognize from this? Our ability to understand the problem. So more powerful to understand the problem. That's why for every new large language model, you can find this: an increased number of parameters.

Other comparison here: the size of the disk, memory usage, and learning. That's why I put this comparison. When you decide to use any large language model, don't look at what the size of this large language model is inside your disk. You have to consider the training data size, because this is an inference process. When you generate, you need more space. When you fine-tune your model, you need more space based on your data. So this is a parameter that you have to put in your mind when you decide to use a specific large language model.

So this model actually was very interesting to me, because it was the first time to read about this model. It's trained on 100 different languages, and in 20 different programming languages, with also scientific papers and mathematical expressions. It's very interesting actually to train the model with these types of data. And also, it's a base for another model called Med-PaLM, where it's fine-tuned with enhanced data. Using this model, they tested on one of the US medical licensing tests. What is interesting for this model is that you can feed this model with an X-ray image, and then it outputs an analysis report based on the findings of this X-ray. It gives a very interesting result actually.

So one of the important things when you choose your model is to check what the trained input of this model is, how this model is trained. This is one of the criteria when you have specific data and want to deal with this data. Find the model which is already trained on this type of data. Then you can use it as fine-tuning in your task.

Tokenizer: how many tokens? How does the large language model tokenize or create a token as an input for the model? Every large language model has a different tokenization technique. That's why you notice that in the previous demo and in this demo, when you choose a model, you have to choose its equivalent tokenizer. And I found this online that shows you different LLMs and different tokenizers. Here you put your text, and then based on which model you choose, you have the number of tokens. Each model uses a different technique and produces a different number of tokens. So if I use one of the pre-trained models here, open source, it will choose 43, for example. Another model, maybe 48.

Does the number of tokens make a difference? Yes, the number of tokens makes a difference. How? Price, they're going to be different. One of the criteria is the price.

Another criterion is: how do you deal with a large language model? You give a prompt and you receive an answer. You feed the model with the prompt. This is how we communicate with any large language model, using the prompt. You send a prompt and then you receive the answer.

Now, if the number of tokens decreases, what do you mean? A more contextual answer of my query, of my prompt. This is related to the second part actually, to understand why tokenization is important. It's related to the next criterion, which is the context window.

So what is the context? This is another parameter that we can use to identify which large language model we use. The context is a combination of the number of tokens of the prompt plus any previous conversation. This is a combination of those. This is the context window for any large language model. So more context window means that you can have a long conversation on a specific topic, so continuously reasoning on the same topic with a huge number of tokens. This is a survey of context windows of well-known large language models. As you can see, Gemini has produced a context window of about two million tokens. I tried to find out two million tokens, approximately how big? It's about 3,000 pages. So it's very huge. This is how different language models have different context windows. So more context window means a more powerful large language model. This is one of the actually important criteria.

So we have different types of large language models based on the training data. ChatGPT, for example, is one of the large language models which we call a general-purpose large language model. It's trained with different types of data, general, multimedia from the internet, so you can ask this model any questions, so it can answer your questions. So this is what we call a general-purpose large language model.

Some other large language models are trained on instructional data. So the training data is something like tutorials, manuals, guidelines. This is the type of data that these models are trained on. So this is good for step-by-step guidelines. If you ask this large language model about step-by-step guidelines, it will work well for this type of task. Many models have this ability, but again, look at the training data. When you expect some output from a large language model, look at the training data of this large language model.

Some other models are trained on conversation dialogue, chitchat dialogue, customer service dialogue. They are trained on this type of data, so they are good for this type of task.

And domain-specific models, which is your assignment task model, where the model is trained in a specific domain, like the medical domain, health domain, education domain, and legal domain. So here, the model is trained only on a specific type of data. One example is BioBERT, which is trained on medical data. So if you have medical data, search for a model that is trained on medical data. If you are working on legal data, look at which model is trained in the way that you want.

Now, what is ChatGPT? ChatGPT is one of the famous, but it's not a large language model. ChatGPT is not a large language model. ChatGPT is built on GPT-4, which is a large language model. ChatGPT is a fine-tuning of GPT-4 now. It's the latest one. How do they fine-tune? They fine-tune to make a conversation. You send the prompt, and then it gives you the answer in a conversation way.

How were they trained? This is high-level because GPT, I think starting from GPT-3, is not open source. So you don't have the exact code. For example, some models are open source. You can find the code, how they trained. The code is huge, and if you want to reproduce it, you need huge computational resources with long time, but the source is there. You can download it, update it, change the instruction, you can do whatever you want. But GPT is closed, it's not open source, so we don't know exactly how the training process happens, but this is from the high level.

So it starts through the steps. The first step is the GPT model, which is a pre-trained model, trained with a huge amount of data. Once it's trained with a huge amount of data, it means it has a lot of knowledge about the language. And this is the start one. And then they fine-tune it in stage one, where they prepare a pair of a prompt and response. The human work, feeding to a GPT model with huge data in the form of prompt and response. And this happens using what they call SFT. It's a neural network that is trained on this objective.

And then once they have this, they have the second step where a human also evaluates and ranks the response of this model. So they train the model, feed the model with a prompt and response. Huge labeled data by the human. So they train the model using this data. And then they run the model again and get the answer for this prompt. And the human works to rank this answer. This is what we call the reward model. And then they feed this information to a different model, we call it the reward model, to train the model to find out which best response is on this. So after the human ranks the output of the first stage model, they rank this data and feed it again to another different model, which is the reward model, another neural network, to train the model for a good response. How to produce the best response related to this specific prompt, after the ranking. So the rank should be high in this case.

And then in the last stage, this is where you use reinforcement learning. You may be better than me in this part, because I don't have any actual background in reinforcement learning, but they use reinforcement learning techniques to optimize the cost based on the reward. So this is a high-level idea about how ChatGPT is trained. It's the fine-tuning of GPT, where they train on huge data, humans prepare this data, this is the prompt, and this is the best answer for the prompt. And then they train the model, and then at the next stage, they run the model, give the model some prompts, and receive the answer. And then the human ranks this and feeds it to another model, the reward model. This is what we may call reinforcement learning with human feedback. Here they use an optimization technique in the reinforcement learning. I think they call it PPO. What is this reinforcement technique? What's the objective of it? You deploy the agent in a real-time environment and it collects rewards. So using the rewards, the output of the reward, they try to make an optimization between the output of the reward model, to optimize the first one. So you can give the model a question and then you can get results. This is very high-level.

But what about the modern training lab? They use a different technique actually. ChatGPT-4 and Claude use a different technique. ChatGPT now, the difference in ChatGPT is they are multi-modal inputs. So they now work on text, work on images, work on audio. So this is one of the important features in ChatGPT-4.

But also Claude uses a different concept here. The concept here is called Constitutional AI. They have some principles (Constitutional AI means they have some principles), and they identify these principles. They say, "be not harmful, be kind", a principle like this. They train according to these principles. The main idea here: they found out that in the previous technique, there is a lot of human work. And this human work is expensive and it's slow. So what they try to do here is to make the model evaluate itself. According to the principles, they run the model and let another model, or the model itself, evaluate its output related to the same principle, whether the model sticks with the same principle or not. So they get rid of any human work during the training. And this is essentially how the Claude model and similar models are trained. They try to get rid of all core human work. Because here we have a lot of human work. We have human work here and human work there. So they get rid of the human work and they let AI evaluate itself. So after the model gets the output, now try to rank this output based on the same principle, which is the constitutional AI. This is how they train now.

We have limitations in LLMs. Yes, we still have limitations. One of the common limitations is the cutoff. Is it still a limitation? This was a limitation in 2023, for example. But now, do you feel this is a limitation? It's one of the limitations? If you ask any LLM about something that happened yesterday, you will get a response or not? Yes, you will get a response. Before, at the beginning, you would not get a response. "I am afraid to tell specific facts", remember. Now, while this problem is mitigated now in a workaround solution, still the models are trained on a cutoff, it's static. The model itself is static, there's a cutoff date. But they try to mitigate this with a workaround solution. One of the workaround solutions is the model can now connect with the internet, search on the internet, and give you the information. A mitigation, a workaround solution. But there's still a cutoff for each one.

What else is static? And actually, this workaround solution brings another limitation. Like what? Now, they search on the internet and get some information from the internet. What about unreliable information? What about noise? What about how to integrate this information that comes from different resources? This has come up with a different limitation. And it's still open for research.

Now, the other limitation, which is limited context size. So if you consider this is a limitation now, with 2 million context size, which is 3,000 pages, maybe it makes 4 or 5 books, something like that. Do you consider it is still a limitation? The new research proves that there is a limitation of this huge number of context size. One of the problems is they found out that large language models are good with the beginning of the context and the end of the context. In the middle, they cannot find or they cannot figure out the information, or extract information from the middle. When you have a long conversation with a large language model, they correctly remember the beginning of the conversation and the end of the conversation. In the middle, the performance is not very good. And this is proved by a lot of research.

Another problem here also is that with this huge number of context of information, it can affect your output. If you search for a finite piece of information, it will be very hard. The large language model fails at this. So it is still a problem, but in a different way. At the beginning, yes, context was a problem. So the feedback when we used ChatGPT in 2023, we had limited feedback when you sent a prompt. But now with 2 million tokens, it's huge. So you can make a big or long conversation, and the idea is we focus on the topic. This is a very good example. But still, we have some issues with this, and still it's an open topic for research. Still, you have to wait, especially for the closed source, you have to wait, which is also another limitation.

A different limitation here, or you can say challenges which exist right now: consistency. Still you may have inconsistent feedback. Not like the beginning, of course, it's improved. There's a huge improvement, but still sometimes you get, "what is this answer?" We still have this situation until now.

Lack of a specialized domain for a large amount. Most of the big large language models are general-purpose. The specialized large language models are not huge like the general-purpose. So they invested more in the general-purpose large language models. Why? Because the specialized is limited, not everyone can use it, so they don't invest in this part.

Lack of transparency: it's a huge amount of parameters, very deep neural network, hard to understand its principles.

Privacy and security. Everything you know, all you have to know is in ChatGPT. I told my kids about it. They ask ChatGPT about everything. So one of my colleagues, he just asked ChatGPT one question: "What do you know about me?" Then it gave his whole profile. So there is no safe information, privacy now is not right. What about with the images? It comes up with images too. What about copyright? So this is another challenge.

Huge energy, huge environmental impact. Yes, because it's a huge model, 24 days, electrical consumption, this affects the environment. And this is another challenge of using large language models.

High computation. So to run a large language model, you have to have a high computational power computer, because it's a huge model. That's why if there is a workaround solution, then it's good actually, but it's bad. It's a huge amount of computation, the model is huge, the size of the model on the disk is huge, the number of parameters is huge, and you need extra space to do inference with this one.

Right, so one of the workaround solutions to mitigate the cutoff training of any large language model is to augment this model with extra documentation, which here we call retrieval-augmented generation. They try to combine the power of information retrieval, where you can easily extract any information from a huge number of documents, plus the power of generating data. If it's a question, you can generate the answer.

So what is a RAG? A combination of two techniques. We have an LLM, this powerful capacity to generate text, and then we have extra documents that we feed to the model to make sure that the result comes from this specific documentation. So this is the idea of RAG. So we feed the large language model with a retriever, a reader. So the reader here is a large language model. And the retriever is external. We feed the model with this external documentation to make sure the answer to our question comes from this specific document.

How can we do that? The first step, again, we have a huge collection of documents based on your domain, medical domain, education domain, based on your information. You collect the data and then you divide these documents into chunks. And then you can use an embedding technique to have a vector for each chunk. So this is how retrieval-augmented generation works. So when you collect the data, you have a technique (it's very simple and it's open/available) to divide it into chunks, and then you have an embedding vector representing each chunk. Which embedding technique? This is another issue. You can choose from different embeddings.

And then the user/client can ask a question. With a combination of this data store, where we embed all the documents in a vector, we call it a data store. And then we feed it to the large language model to get the strengths of the model that generates a coherent answer, and then go back to the user.

And this is another architecture for a question-answer system. We take a large language model where the documentation is stored, which is based on information retrieval. It's one of the NLP parts.

Now, in detail how it works: we have a question. You feed the model with the question. We have a retrieval, the same idea starts here. And we have the external knowledge, which is your documentation prepared as a vector. And inside the retrieval we can encode or embed this documentation in a different way. How do you create this high-dimensional embedding vector? Many metrics: Euclidean, cosine, KNN, inner product, any metric. You can save this information, index this information using a retriever. So the same retriever will now return a context rank. And this rank will be fed to a large language model, any large language model, to generate answers. So the answer now, it will be generated, but generated based on my context, my knowledge. We will use the advantage of the large language model with high knowledge of the language, how the structure of the language is. This is what the large language model knows: high knowledge of the language, structure of the language. I will take this as strengths, but I will let it generate the output based on my external documents. So these are the two common components: a retriever and the reader. The reader now is a large language model, but the same idea of reader and retriever.

OK, so this is what actually happens in large language models. We have a document corpus, huge. We divide it into chunks. And you decide what the size of each chunk is. We have different techniques for that. And then we feed it to the embedding technique, where each chunk is converted to high-dimension data. And then this is the important role of the vector database.

So what is a vector database? It's the first time after a huge involvement or the development of AI, we always hear about database. What is a database? We say structured data inside a database. So what's the difference between a vector database and a database? We will not say structured data. We will say vectors and embedding vectors. And the power of the vector database is how to index these vectors. So when you ask a question, how do you retrieve this data? Very fast. So the indexing technique is very, very important. And this is how we differentiate between one vector database and another vector database: the techniques of indexing the data. Because when I retrieve the data from a vector database, I will not search for an exact match like a database. When you have a database and you have a query, you search for an exact match. But here I will not search for an exact match. I will search for a similarity. So how to take this embedded chunk and index where similar chunks will be near to each other. And when you have a question, how do you find that similarity between the query and the chunks? And this is the role of the vector database. So it's completely different from the normal database. And we use it now, after this era of AI.

So the main part of the vector database: how they index this documentation and how they retrieve this documentation. And this should be very, very fast. The optimization here is very, very important.

And then, we use a database, we index the data, and then we have a question, a prompt. There is some optimization searching technique. When we search for this huge number of data with a specific task, which is a prompt, then we choose which vector retrieves the chunks. And then return it back to the prompt, and this is the answer to your question. This is the life cycle of RAG. And each of these cycles, the same cycle, not changing, but you just, in your code, just choose which technique you use. And the techniques are available. You will not write code from scratch, but you should really understand what is going on at each step.

And then, now, how to create this chunk? One technique is a fixed-size chunk. I fix a number of tokens, number of characters, and this is my chunk. Or another technique is to have an overlap. This technique is fast, easy to implement, but it has some drawbacks. What is the problem with this technique? As it's fixed-size, so maybe the answer will be on the border, for example. Or what do you think is the other drawback of this technique? Is there any semantic behind this definition? No semantic meaning.

Another improvement technique is based on some semantic meaning. So I divide it into a specific number of sentences, or divide it based on paragraphs. So the paragraph would be my chunk. The second paragraph would be my chunk. Or I can use an overlap to make sure that the answer will not be on the border. This has improved the technique because now I divide with more semantic meaning, but at the same time I have a problem here: what about the size of each chunk? This is another problem that vector databases have to deal with. But this is the technique for how they divide into chunks.

Then, to communicate with any large language model, we have to create a template prompt where you have a question and then you get the answer. And this template is just copy-pasted for any of your questions. So the template prompt is "answer the question based only on the following context", and this is the variable, so anything between curly brackets means it's a variable. And then the answer based on the content is what we retrieve the data from a vector base.

And then this is different types of vector databases. As you can see, some of them are open source, some of them are closed source. Some of them are dedicated as vector bases. Some of them are improvements: they are well-known databases, MySQL, Postgres, and because they recognized the importance of the vector base, they include it now in their framework. So you can find a vector for Postgres. And I think the common one is Chroma. I think this is the top now, Chroma, as an open source, and we can use it. So this is one of the vector bases that you can choose for your system. But these are different vector databases.

Now, how to measure the quality of your RAG system, the question-answer system based on RAG? We have two main criteria. One of these quality criteria is the returned vectors or returned chunks. We can choose the top. We can rank it. And we can make a cutoff to a specific k. So top 3, top 4, top 5, top 10. And we use this to have a measurement for the quality of the output. One of these measurements, we call it Normalized Discounted Cumulative Gain at k, NDCG at k, at a specific rank, where k is the rank. So top 5, top 10, based on your choice.

And how do we compute it? We compute it with this equation: summation from 1 to k, relevance at i, divided by log 2 (i plus 1). OK, so the rank is different from the relevance. The rank is the output coming from your model. But relevance exists between whether the chunk is relevant to a question or not. This is your role in your assignment. This is the technique that you test. You get the chunk, how they return it, and then you rank this chunk, which one is relevant. So the rank may be different. So one of the documents comes at rank one, but you find out that this is not relevant. I give it two as a relevance. I give it three as a relevance. So relevance is different from the rank. So the difference is three.

All right. So we can compute it by this equation, where li is the relevance score at position i, and the penalty of the lower rank, we divide it by log (i plus 1). I have a penalty: this document does not have a correct relevance. Right, so the value is between zero and one, between rank and relevance.

So rank: when you have a question-answer system, you have to decide how many documents it returns for you. As we can see in my example, it returns with a lot of context related to your question. So it will not return just one context related to your question. With your parameter, you can ask your model to return the top 10 documents related to my question. And this is not enough. With a huge amount of documentation, you may find a different document answering your question. So this is what we mean by rank. They return different, and consider if you choose 10, these are the top 10, based on the model rank. But when you answer, when you see this documentation, you may have a different rank in your opinion as a user. This document which is ranked 1, it may not be ranked 1. It may be ranked 3 or 4. And this is ranked 1 or 2. So you, as a user, have a different rank. And this is the difference between ranking and relevance. You decide what this document's relevance is to your question. And using these two pieces of information, we can compute this as a measurement of the performance of your RAG.

If the rank is perfect, it means this will equal 1. If the rank and relevance are the same, this will be 1.

Now I try to give you an example. Actually, I try to understand what's going on. So I go through an example step by step to understand it. So I have five document chunks, chunk 1 to chunk 5. And this is the rank based on the output of my rank model. And this is my relevance rank. So as you see here, the first chunk is ranked as one and its relevance actually is one. The second one is ranked at two but its relevance is three. So relevance may be different from rank. Then I apply the same equation. So I compute the equation for each one, compute the total. As you see, the total of this is three. And then we sort relevance in descending order. So ideally sorted relevance is this in descending order: 3, 2, 1, 1, 3. This is your relevance. Then I computed the ideal DCG based on this specific rank. And then I have the total ideal DCG. And when I divide the first by this, this is the evaluation. The final value of NDCG at a specific rank. And this is how you compute. Any questions?

And you can mix it up with your knowledge. And they try to eliminate the human. But for your project, I think I remember that I asked you to test your model. You have to extract this and manually check the output. So you can do the same, one of these techniques.

Another technique actually, RR: Reciprocal Rank at K. Another technique is Precision@K. A different quality measurement to measure the quality of a RAG system. The absolute number of relevant items in the top K, divided by K, is a different measurement.

When you create a large language model, we have two important parameters: temperature and top P. What is a temperature? How much is it deterministic? How much your model will be deterministic? And top K? We use in large language models. So these are two important parameters. You have to keep them in mind when you create or deal with any large language model.

Frameworks: we have many frameworks available to solve this task. Many of them are open source. There's a very famous framework for this specific task, and you can use it. Haystack, which I just explained a few minutes in the previous lecture, Haystack is one of the open source frameworks where you can implement any RAG system. They have a vector store, they have a document store, vector store, document data, large language model, you can choose from either. So there are many, but I think LangChain is a famous one. It's very powerful for many reasons actually. The reason comes from here. This is the ecosystem for LangChain. You can find here 157 different document loaders. A huge number of document loaders you can choose from. You can have only 50 or something vector stores you can choose from. Large language models. A lot of tools and techniques all in one framework. That's why LangChain is very popular in this specific task of RAG, because it's an ecosystem with a lot of tools, a lot of techniques in one place and it's open source, free to use. You can download it easily and then you can work on it. Choose between all these things. That's why I recommend using LangChain, but it's up to you to solve your problem.

There are open source large language models. This is an example of open source large language models. You can download them on your computer and work on them. Some others are not open source, so you have to pay to access the API key and you have to pay per token. But for our assignment, there's no need to do that. You just try to work with any open source LLM. Even if the performance may not be good, I prefer to work with this.

This is an example of an open source large language model. And this is how to decide which large language model can fit on your computer. So I tried to make this comparison. If your computer is 8 gigabyte RAM, then this is a perfect version, because for each large language model, we have a different version. So I think this is perfect for anyone who has a computer with 8 gigabyte. Then you have a more powerful computer with 16 gigabyte of RAM, for example. Then this is maybe good, with this more powerful computer you have a large language model to work on.

And as you see, all of them work on Ollama. OK, so this is the Ollama command to grab this model. I think you have this experience with everyone when you use Ollama. Yes? You can use it in another one. So what is Ollama? You can download your large language model and make it work locally on your computer.

We have different environment systems for large language models. We have Ollama, it's one of them, which allows you to work with your large language model. Another one which is similar is LM Studio; it is the same, but Ollama is more popular than that. But they are both free and work locally on your computer. Another, which is cloud-based: vLLM is cloud-based, Hugging Face is cloud-based.

Which one do you use? I'm not talking about the assignment, but I'm talking generally. If you work in a company, which one do you use? It's based on the business logic of your company. Like what? If you work with a service provider, you will not go with local. This is one of the signs. If you want full control over your model, you want to go with local. If you want to work with your application from anywhere, go with the cloud. If you want your application automatically scaled up or scaled down based on the number of users, go with the cloud. The cloud provider gives you the facilities, gives you the service to scale up automatically. If you have a huge number of users, it automatically gives you more resources. Automatically. Scale up automatically, scale down automatically. But if you work locally on your computer, you need extra storage, extra resources in your computer to scale it up, for example. So it's based on your application, the business logic, what you want to do. You can work locally. You don't need to access the internet. You don't need the internet to work with your application. But in the cloud, the internet is essential. So it's based on a task, your business logic. Choose which one you go with: local storage or the cloud-based environment.

This is a comparison. I didn't include here LM Studio because it's the same. It is open source, and they are local. But here, I gave some differences between LM Studio, Ollama, and Hugging Face. So this may help you choose which one you can use based on your business model and your application.

Open source: this is LangChain, open source. I think we use it, so you don't need to adjust to this.

And this is actually two important slides. Now, how can we compare between different large language models with a huge amount of language models introduced every single day? How can I choose? We have two different benchmark comparisons. The first one is a leaderboard, community driven. I can show you here. It's updated every day based on the community feedback of using these large language models and their thoughts. So it's community-based, it's not based on benchmark standard metrics, but it's a community-based ranking. So here you can see, for example, the model Claude is at the top, and how many votes for Claude based on if you use Claude in your task, then you can go and vote. And so, there is a category here. So in math, for example, you can find the model is changed. The rank of the model is changed. So in a math task, the rank here is changed. In instruction following, instruction tools, maybe this is different. In coding, for example, coding tasks. So in coding, you may find a different rank between different large language models. So to choose which is the best large language model, it is based on your question. Invest some time to make a search, to find the best large language model that fits with your task. ChatGPT-4 is the best one, but maybe it's not good for your specific task. So this is one of the best things: how to choose. It may help you.

The other important one actually, it's more standard, which is called Holistic Evaluation of Language Models (HELM). This is done by Stanford University, where they test the model based on the benchmark data. OK, so it's not community, it's not voting, but it's based on this: it's the model with benchmark data. So it's the same model, apply the model with different benchmark data and then rank the model based on this. And there are different criteria actually to rank this model, which you can ask HELM based on. Which model is safest? Which model is more powerful in any field? From an ethical point of view, which model preserves privacy? They rank this based on this data. So I consider HELM as the standard and the more professional website to look at the rank for existing large language models.

So here, you can see this is based on context. GPT-4 here is in the book. The mean is called, and also other scenarios. OK, so in a different scenario, you can see here. This is course descriptions. So it's different, but this is a standard way to look at the performance of up-to-date large language models. So I recommend you to go with this point, while the community one is also acceptable. So this is an important thing that you have to take care when you choose your large language model. Don't use any large language model because it's famous or new. Look at this benchmark. But we have different dimensions. They rank the model based on accuracy. They rank the model based on fairness. So we have different ranks based on fairness. And you can find this on the website: bias and efficiency, different criteria you can use to evaluate language models. Any questions?
