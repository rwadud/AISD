This competition closed in 2009, and there was a developer team who won this competition where they increased the accuracy about 10%. 10% is a huge improvement. They took the prize, and after that, Netflix never integrated this system in their recommendation system. Never used it.

You think what? What do you think? Is it complex? Very complex. The recommendation system is an online system. When it comes to the development, the deployment, there are some important factors. Look at the actuals. The latency, response time, is very important, especially if it's a real-time or streaming application. Latency here is very, very important. Scalability. Is this model flexible? Does it scale up when the number of users increases? Usually Netflix has the number of users increasing. Then this model would not satisfy all these important constraints when you think about deployment.

So, why did accuracy increase by 10% but they didn't integrate this model in their recommendation system and they stayed with the old system? In a deployment you have to think about different considerations based on your task and based on the nature of your task. This is one of the very famous real examples that happened in 2009.

Now, if you have two models like this: you have model A, the accuracy is 99%, the runtime is two seconds, so the latency or response time is two seconds, the size is 125 megabytes. And you have another model, the accuracy is 97%, runtime 0.1 second, and the space or the size is 10 megabytes. Which one do we use? We use model B. Why? There is a difference in accuracy, but it's not very significant. There is not a very huge significance of dropping in the accuracy between the first one and the second one. We prefer this because the latency, the response time, is very hard, which is highly recommended for any online system, and also the size, the memory. The memory is not just memory. If the model has a small memory, it means all the computation costs will be reduced. All the power cost will be reduced. Many, many factors will be reduced if you have a small model.

So, after 2017, there was a huge growth in using transformer with a more complicated transformer architecture, more layers, the number of parameters increased. They started to think about, we need something else. We need to improve it in a way that we are able to use this large language model in your device, where there is a limited memory size, or use it as an embedded system, where it's a part of the whole system, so we need a small size of this. Under one condition, it should be cost-effective, memory and computational power and everything, efficient enough. We're not putting a useless application or useless model. This is how we can reach customized or acceptable level of accuracy.

This is the big question. After all the huge improvement in large language model, producing a very complex large language model needs GPUs and high computation power to run it before you can use it. So what we come up with as a solution is model compression. In model compression, we have the large language model, which is the original model, and we try to compress it to a small model. But we keep the three important parameters. In the same way, we can have a model to be used to predict the outcomes, but with acceptable level of decreasing performance. This is our conjecture now.

How can we do this, and what are the famous techniques to perform model compression? We have quantization, pruning, and knowledge distillation. These are the three common techniques used now to compress the model. Until now, it is not the standard technique. Every technique of this, they started to improve it. Every week, every month, they improve these techniques. So it has not come to the standard, but these are well-known techniques and already used in some systems.

Let's start with quantization. What is quantization? To understand what quantization is, most of you come from engineering background or computer science background. Do you know what a good representation is? In a computer, what is the real number in this form? This is a technical form. The first bit is for sign, then we have a mantissa or exponent, and then we have the mantissa. How many bits for exponent, how many bits for fraction or mantissa, depends on representation. The common representation is either 32 bit, so each floating point number can be represented in 32 bit. Any real number saved in your computer in 32 bit code.

What does this number represent? The sign represents, of course, the sign of the number. Exponent represents the magnitude of your number and the fraction or the mantissa represents how many decimal places or precision of the number. Precision is how many numbers after the decimal point. This is what the floating point representation represents.

Now, we have different techniques. Sometimes when you download any application, it asks you which your system, 32 or 64 or whatever. This is the difference. For 16-bit floating point representation, we have one for the sign, five bits for the exponent, and 10 bits for mantissa. For 32-bit, which is the standard, it's one for the sign, eight bits for exponent, and 23 bits for mantissa or the fraction. For 64, we have more bits for exponent, 11, and 52 bits for mantissa. This is usually in numerical computing techniques where the simulation of the aerospace or something like that, where the precision is very, very sensitive in this case. This is a type of the computation to use a 64-bit, more sensitive, more important application.

Now what happens if we just reduce the precision of any number? Instead of having 32 bits to store the weight of each, you know we have the matrix and each in the matrix we have a number and this number is the weight and this is represented as parameters. What if we just reduce the representation? Instead of the standard which is 32 bits, we may choose 8 bits or 16 bits, whatever, just to reduce that number. What will be the effect of this? Less memory, space. Perfect. And what happens if there is less memory space? More fast communication. So it's a huge benefit. So you reduce part. What is the cost? It is the accuracy. This is the balance that we try to do. How can we do this while keeping the accuracy at a reasonable level?

This is the idea of quantization. Playing with the floating point representation of the model, we can come up with a small model with reasonable performance. These are different quantization techniques. For the same model, which has 65 billion parameters, when it's represented in 4 bytes, this is the size, 260 gigabytes. The same model with the same number of parameters, when you represent it in 2 bytes, it takes only 130 gigabyte space. And so on until the most promising techniques, which is one-bit representation for each parameter. I will explain how we can do that. But imagine the huge reduction of the memory space. As a consequence, any computation, everything will be reduced while we keep the performance at an acceptable level.

This is what we can do with quantization. The quantization idea is to reduce the floating point representation of the weights for your model weights or model parameters. We are keeping the same model as it is. We just play with these parameters. This is a huge improvement in the computation. For example, you can see here for this model, TFLOPS is the Tera Floating Point Operation Per Second. So if you represent that number for your weights in 64 bits, this is the number of Tera Floating Point Operation Per Second. When we reduce it to 16 bits, this is the number of Tera Floating Point Operation Per Second. The difference here is huge, the computation power will be reduced, I can make more operations per second than here, because we reduce the sums, and everything will be reduced based on this. This is a huge difference.

Now, do we have a technique so lower precision means faster processing? Yes, this is the consequence of reduced numbers. Because in a low level, in a computer, you deal with bits. If the number of bits is small, it means the computation, or the time to complete this operation, will be at the lower level of your computer. So lower precision means faster operation, faster process.

In PyTorch, we can do that. It's very simple. We can have a quantized model. It's a torch quantization dynamic. This is how we can apply quantization, with two important parameters. The first parameter, you have to specify your quantization level. For example, here I choose to represent in 8 bits. The same model will not change the original model. We will have just another model with a small size, quantized size to fit.

The first parameter actually is very important. What do we mean by nn.Linear? We cannot quantize all the model parameters. Some parameters, we have to keep, because it will reduce the performance. So which parameter or which weights can we reduce? Usually the linear matrices. For example, the matrix for Q, for a transformer, we have Q, V, K, we have all such matrix, huge matrix. We can apply quantization for all linear matrix. But for example, the activation function, can we quantize activation function? Activation function already has small numbers. Don't touch this, the weights of this function. Another layer that we are not able to quantize, like the embedding layer. We need the precision of the embedding layer, because this is what we learn from the language. Some layers will be quantized, some layer will not be quantized. Usually we quantize the linear layer, or linear values. That's why we enforce nn.Linear. It's very important to choose which layer you can quantize, while keeping the others as is, to balance the performance of your model.

What about binarization, which is, for me, I wonder how we can do that. Binary means that means one thing. How can we save the parameter or the weight in one place? They came with a very intelligent technique, which is, for example, if you have this weight, floating point number represents the weight of the parameter in your model, then they apply a specific threshold. This threshold is 0, so anything greater than 0 is converted to 1, anything less than 0 is converted to minus 1. Then we come up with a binary weight, 0 and 1. It's a huge improvement. Why? Now we can use in our operation multiplication addition, we can use a bitwise operation. You know what a bitwise operation is? We now work with the lower level of the computer. We're dealing with the bits. This improves the accuracy, not only the accuracy, but the computation speed. Of course, we can now work on the bit level. So it improves the computation speed.

This is what they did. We come up with binary weights, which they proved in their paper. For example, if you multiply specific x values, the three values by the new weights here, the total here, summation of Wi*Xi, for specific Xi, 0.5, 0.2, 0.8, the total is 0.23. While we apply the same 0.5, 0.2, we keep X as it is, the summation now is 0.26, which is not too high. This is their idea, how they can use a large language model in limited memory devices, like mobile or an embedded system.

Now, Microsoft in 2023 improved this project. By the way, every year they have a new model. They used binary quantization, and then they added another technique to keep the performance high as it is. They introduced something we call a scaling factor. The same idea: this is the original weights, then I have zero threshold converted to plus and minus, but at the inference step, or at the end of your calculation, they multiply all the finalized values by the scaling factor to approximate the original weights. After I finalize the original weights to 0 and 1, I can have a scaling factor when I multiply it back with this binarized value, I can approximate the original weights.

How do they compute the scaling factor? For this paper, they compute it as an average of all the weights. This is your scaling factor. For any matrix, to compute its scaling factor, just average all the values of this matrix. This average, they consider as a scaling factor. Keep in your memory, keep the value as it is, plus and minus one. At runtime, they call it in the paper, the on-the-fly computation. At runtime, when you're in inference, just multiply with this scaling factor, and the scaling factor should be 32 or 64 by the floating point representation, multiply with the scaling factor. Did you get the idea? Any questions? This is what Microsoft introduced. Actually, they have a different way of doing it. But this is an open topic for research. Until now, every time the research team is working on a new technology.

What is distillation? Distillation is that we have the original LLM, which has a huge number of parameters. This is the idea of distillation: how we can, from a big or large language model, have a small large language model where we can use it in small-size machines. Is the idea clear?

This is the idea of pruning. Its idea is to remove parameters from the model after training. In a matrix, we rely on matrix multiplication. Some weights have a significant magnitude. We consider these important values. Some weights are very, very small, so we can neglect them and the performance will not suffer. For example, we have these weights, they compute the absolute values of these weights and we can neglect some of them, and then all the small values, just replace these weights by zero.

This is the empirical effect of doing pruning in different models. As you can see here, the most examples of the models here are related to machine vision. This is how the compression of the model affects the model size. Here as you can see, if you have a rate of pruning, based on your threshold, you decided to make 30% pruning or 50% pruning or 100% pruning, means everything is zero. This is your rate, it's your choice. This is how the pruning affects the model, the performance or the accuracy of the model. As you see, when the rate increases the performance goes down. From this picture, you can see here, this is the rate or amount of pruning. We usually consider the safe area is from 30% to 50%. Don't go higher than this when you decide to use a pruning technique. Based on all the performance and empirical calculation that we find, from 30 to 50 it's a safe zone, where the performance of the model will not be degraded. Some models you can see, I think, model 8, 8X, the performance degraded when you apply pruning. You have to think about which model you apply pruning to and what the effect of pruning on this model is.

These are three common techniques in compression, and this is the difference between them. Quantization: no parameters are changing. We just change the precision, how much space in your computer you save for the parameters. Pruning: the number of parameters are set to zero based on your pruning rate, but the rest will not be changing. Distillation: all the parameters are changing as we have here. This is the difference between the three compression techniques. Any questions on the compression techniques?

"I have one question about the slide you showed us for teacher model and student model. For training, we have some inputs, we have some outputs. Are the outputs going to the student model and at the same time the student model has access to the data, the original data?"

Yes. The same data, we train the smaller model, small size model, number of parameter, different architecture. The first input is the same data that we fed to the larger model and the output or probability distribution that the larger model added to all the classes. This is a two-input for the student model to try to imitate or mimic the behavior of the larger model while we use a small size model. Any questions?

Now, I think we need to finish our session. Anyone who is familiar with these two concepts? These are two important concepts: transfer learning and fine tuning. What is the difference? What's the difference between transfer learning and fine tuning?

"Fine tuning, you're training the original model. So you're essentially training the original model with data that we worked on in a specific area. Whereas transfer learning is essentially you're training an entirely new model using some original model with the original training data that we've created."

Original data? What's the data that you use for training the original model? You are very near, but I need more, I need reference. When you perform learning, for this new model, you're not training in the same way from the modern model?

"In transfer learning?"

Yeah. Then what happens during the post-production? What do you do? You take the original data and what you do is this original parameters.

"FC unit?"

FC unit means what? Adding extra layers or changing the parameters, or some other ways.

"Changing the parameters?"

Yeah. All right, so you have some time? Yes?

"I think it's the impression we just made, the new model to predict the answers as close to the original model."

Your answer is very, very near. So let me tell you something. I would say on transfer learning, I would give you an example like what happened with GloVe. When you train your model with GloVe as an embedding layer. How can we do that? Do we train GloVe from scratch? Right? But what we have in your assignment two. In GloVe, we have, for example, because this is a very clear example, a glove embedding file, where you have all the vocabulary in English, and for each vocabulary you have the vector representing this vocabulary. Now, you want this as an embedding layer in classification tasks. What you did in this case, can you give me a scenario? Any assignment problem, it doesn't matter, just any problem. What you did for your own data, you look up for the vector based on your data, and then you can get your feature matrix. This feature matrix, at that time, if you have a sentence, you just get the vector for each word or token in this sentence, average them if you want to have one vector for each sentence.

This is an example of transfer learning. We do not touch the GloVe parameters. So in transfer learning, like BERT, for example, as an embedding vector, BERT generates that embedding vector, each one, in all English. We freeze this layer, and then if we use BERT for classification, we build another layer on the top to do the classification. We freeze that model's parameters. This is the idea of transfer learning. In transfer learning, we freeze the model. We just take a picture from this model, then we use this representation for our model and then run the approach.

But in fine-tuning, we will change all the model parameters with our own data. The huge parameter or the huge model has parameters. Then using fine-tuning, we need to fine-tune this large language model with our own data. This is what you get in your assignment two. You have your specific domain data, and you get benefit of the large language model understanding the language, and you finally change all the parameters based on your data. The benefit here, you will not start from scratch. You will just feed your model with your own data and let the model adjust its parameters based on your specific domain data. Is that correct? You can imagine what happens.

This is the main difference between fine tuning and transfer learning. In fine tuning, you change all the model parameters. This is not an easy task. This is what we call supervised fine-tuning. Supervised, why? Because we have our own labeled data. We need to change this huge, large language model with our own data. We feed the model. This is the original model. Then we have our own data. We apply supervised learning to change all the model parameters. So it's a trainable layer, not like one of the settings no_grad. no_grad means not make this layer trainable. We freeze this layer. But here, we need to train. The training will be applied end to end. I will not freeze any layer. All the parameters will be changed based on my data.

What is the issue in this? As it is a large language model, I have a lot of challenges. Large model means a lot of parameters. What happens if you try to change all this during your training of your data? You need huge memory, huge computation power, and a very high quality of data. You have to choose it carefully to fine-tune the data so not to disturb its original weights. This is the challenge of using a large language model and fine-tuning with your own data. You need high memory. You need computational power, GPUs, high computational power. Now, think about we need to use this in a real-life application. This will be a challenge due to all these challenges.

Now, think about how we can deal with these challenges. They introduced something we call parameter-efficient fine-tuning. This research started from 2018. They have different techniques to make this parameter-efficient. The most recent one actually is low-rank adaptation technique, pronounced as LoRA, introduced by Microsoft in 2021.

What is LoRA? It's a low-rank adaptation technique proposed for fine-tuning tasks. What is the main idea here? The idea comes from linear algebra. If you are familiar with linear algebra concept, if you have a matrix, how do we define the rank of any matrix? What is the rank of a matrix in linear algebra? If you have a matrix, this is a known technique in linear algebra. You can compute the rank of any matrix. How do you compute it? Or what do you mean by the rank of any matrix?

"How linearly in a matrix? I don't know."

How many independent rows? And how many independent columns in this matrix? What do you mean by how many independent columns and rows? You can find that some rows are a linear combination of the other rows. But some rows and some columns are wholly independent. You cannot represent it as a linear combination of other rows. This is what you mean by the rank of any matrix.

The idea that they came for LoRA is to, instead of dealing with the weight matrix, we will deal with the rank matrix, a small version of this matrix, and at the same time, it's an approximation for the original matrix, by applying the linear algebra technique, which is computing rank. This is the idea. We have the pre-trained weights with the dimension d by d, with the vocabulary with the vector size. We can reduce it to two rank matrices, A and B. What is the size of A and B? It's the rank size. So this matrix of rank is 3. R here will be 3. The first one will be d multiplied by R. And the second one is R multiplied by d. The same matrix, approximation to the original matrix, but at the same time, lower parameter. And we will not train, we will not update the original matrix. During the fine-tuning, we will update this one. At the end, we just add the values to the original.

This is the result for LoRA. It was very helpful at that time. Very helpful. The computations and the parameters, the efficiency of fine tuning was very high with using this very intelligent idea.

Again, we are not able to apply LoRA for any layer. We can apply LoRA in this layer, the basic weights matrix, that is value and query. We can apply LoRA in different points to keep the performance of the model as is. Instead of working with fine tuning, we can find a small matrix which is approximation of the final one. The speed would be high, no memory, we don't need a huge memory location. Computation power would be less and at the end we just add it to the original one. This is the update that your own data uses to update the original model parameters.

Empirically, this comes from a paper published in 2021. Something that you have to take care of when you use LoRA. LoRA needs a higher learning rate than normal fine-tuning. When you use LoRA, you have to increase learning rate. LoRA poorly works on large batch size. These are two facts that you have to keep in your mind. In the empirical results with a large number of data, they came up with these two important facts. Keep in mind when you use LoRA, keep these two facts in your mind. Again, it is not the final or the standard related to LoRA, but the research works in this field.

Some research actually I like, I like the idea here. They have QLoRA, quantized LoRA. The same idea of LoRA, but they integrate it with quantization. Instead of using 32-bit, use 8-bit or use 16-bit. Very intelligent while it's a very simple idea. Just compile these two techniques. Use LoRA with a rank matrix. Using this technique, you can train a 65 billion parameter model on a 48 GB GPU. Again, this is toward our main objective, which is how we can use such a huge model in small-sized devices. Mobile and embedded systems.

This is all what I want you to know about the compression techniques. Again, this is not the up-to-date information, but every day this is still a hot topic for research. The research will improve each model. Maybe they come with a different technique, different idea, with the same objective: to reduce the size, reduce the computation cost, reduce the memory, but keep the performance at reasonable values.

Now, prompt engineering. I will not talk much about prompt engineering because all of you know how to communicate with a large language model, and this is the language of the communication between a user and any large language model, so it's a prompt. You ask the large language model a question, and your question, we call it a prompt, and then it answers your question, and it's very important to take care about how you form, how you write your prompt, because this guides the large language model to respond with a correct answer. Garbage in, garbage out. Ask the right question to get the right answer. If you ask the wrong question, you will not get a right answer.

These are basic tools for any prompt when you communicate with a large language model. I think you apply this in your assignment two. You have the context, which is very important. You have to provide the prompt with a context, and then you have to provide the prompt with an instruction, and then we have the input data. It's your question, and then give an indicator. What is the output format you expect? This is a very important component that should be included in any prompt. Clearly write your instruction, then you can find a correct answer, a correct response from your large language model.

Benefits of prompt engineering: prompt engineering means how to write a correct prompting to your model and improve performance, work out the chain, improve response quality, enhance interpretability, bias mitigation. Based on the other input, during the final action, you try to mitigate the bias. How to get this? This is a better way to write correct prompting to your large language model.

Prompting techniques, we have many, we have a huge list of prompting techniques. Chain of thought, I think you're familiar with this. Zero shot, one shot, few shots. These are common prompts.

Let's start with the first one. I tried to categorize it into main categories. Example-based prompt: zero-shot, one-shot, and few-shot. We have instruction-based prompt, chain-of-thought prompt, role-based prompt, and persona-guided prompt. This is not all the categories, but I tried to give you famous examples of prompts.

Let's start with zero-shot. Zero shot means you write a prompt without giving your model any example. Just ask a question. For example here, classify the text into neutral, negative, or positive. That's it. This is what we call zero shot. You didn't feed the model with any example. In one shot, you feed the model with one example. For example here, "because I think the food was alright, the sentiment is..." neutral. This is the idea of one shot. Give the model one example of your task. Few shot means give the model more than one example. This makes a huge difference in the performance of the response of this model.

This is actually covered here, they have an empirical experiment about GPT-3, that if you feed the model with zero shot, or one shot, or few shots, it affects the performance of the model. As you can see here, the models with high parameters have huge improvement in the accuracy. If you give it multiple shots, this is a model. For one shot, you can see the improvement of performance. For few shots, where you can increase the number of examples, the performance will be improved. This improvement, especially with a high parameter model, the improvement of performance or accuracy was very high if you fed the model with one shot and a few shots.

Another type of prompt is chain of thought. For example, why is it not correct now? Because this is not correct now. If you just write this prompt, it will give you the answer directly correct, because you can prove the model itself. But the idea of chain of thought is if you want to ask the model a specific question, give it as a chain of steps. For example, if you want to ask such a question, "Roger has five tennis balls, he buys two more cans of tennis balls." This is a complicated question. Any large language model can answer this question correctly now. But I just give you an idea how to divide your question into steps in the chain of thought. This will improve the performance of your model. Instead of having this question, we can divide it into chain of thought. The first part, I ask the first part here, once we get the correct answer, I can build on the top of this answer. This is what we mean by chain of thought, especially for complicated questions. This is a very good practice when you write your code.

This is an example of chain of thought prompt where we can have different levels of prompts. This is another example and how the model can respond to this. This is a common technique in prompt engineering, how you can divide your problem into chain of thought when you create your template, your prompt template.

Role-based prompt: it's a good practice even when you deal with ChatGPT or any other, I tried this by myself, if you put the role. For example, I may ask ChatGPT as a natural language processing professor. It will give me the answer based on my role. In your case, maybe as a student. The role will be very, very important to control the output or the response of your prompt. When you come here to any large language model, try to put your role here like a context.

Persona here is one of the common examples. You are William Shakespeare. If you want the model to generate a story or something like that, give it a specific persona so it can generate the story based on the value of the information from our research. This is a very good practice. One is to create a good authoring, a good prompt. Creating a template, I think, when you have a ChatGPT.

Just a quick question. Your project: I think most of you work on the large language model, yes? In your project. How do you do that? Which model do you use? ChatGPT? It depends on the client. But you deal with the large language model. So you write a prompt, because this is the only way to communicate with any large language model. You know what a prompt is, how to write a prompt, and you have to create a template. That's why I'm not going deep. This is a general template. When you communicate with ChatGPT or any other large language model, you have to create a template, and then fill this template with your variable parameters. I think you are familiar with this because this is how we can communicate with even a question answer system. You have to create a template and then fill the template with your query and the question.

Next, prompting best practice. This is a general guideline about how you will create a prompt. This is how you can create your prompt.

The material of the final exam is up to the end of this lecture. This is the last part of the lecture. I will not add any other information to this content. It's a comprehensive exam starting from week one till week 13 when we finished. Any questions?

Thank you so much. I'm very proud that I have wonderful students like you. I hope that you learned something that you appreciate in the future. I hope so. You remember me. What you learned here actually is just a foundation. I tried to make sure to build a good foundation. But there is more and more, especially in this field. You have to keep yourself up to date with the new techniques. There is a new library, everything goes very, very fast, especially in this area. What I tried to do here is just to give you the basics and the foundation, and then let you fly. I hope for all of you full success in your career and in future work, and see you in the final exam. Thank you so much.
