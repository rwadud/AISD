Last lecture, we covered the n-gram language model. To remember: this is a statistically-based language model based on computing the probability of which word will come next. We divide the text into a sequence of n-grams, and you have to choose n. Then, for each n-gram, we use the n minus 1 gram to predict what the next word will be. This is the equation for the probability distribution for n-grams. We compute it by counting.

For any n-gram, to predict word number three given word number one and word number two, we count how many times the three-word sequence appears in your corpus and divide by how many times word one and word two appear in your corpus together. That is the probability. We then use the chain probability rule to compute the probability of the whole word sequence. So the probability of w1, w2, w3 is the probability of w1, multiplied by the probability of w2 given w1, multiplied by the probability of w3 given w2, and so on.

During the lecture, we discussed the limitations of this technique. We also looked at an example. It's not a complete example, but it gives you some information. If you have a simple corpus like this, how do we compute the probability of "have" given "I"? The probability is how many times "I have" appears in your corpus, divided by how many times "I" appears in your corpus. Counting technique.

Then we discussed RNN as a deep neural network. We also use it as a language model, handling sequence data, and we identified a specific limitation of RNN. What is that limitation? Vanishing gradients.

As you can see here, we start scanning or processing the sequence from left to right to generate word by word. Starting from the left, we feed the neural network with all five words, one by one. At each timestamp, we compute the hidden state and the output for that timestamp, and then we pass it as input to the next timestamp along with the next word. The nature of this is that, at the end, the answer depends on all the previous information. At each timestamp, the network propagates information forward to the next step. This improves the performance of the language model compared to the n-gram approach.

We then had a problem related to RNN, vanishing gradients, so researchers began to think about another architecture that could address this problem. They came up with the LSTM, long short-term memory, which has three gates. This is the main feature of the LSTM. We have a forget gate to filter the state, deciding which information to add based on the current timestamp. We have an input gate to update the state, and we have an output gate to generate the output.

The main difference between RNN and LSTM is that we have an extra input: the previous cell state, which holds some memory from the previous step. At each step, we can decide which information to add and propagate to the next state, and which information to forget and delete from the current state, because it isn't important. The input for each LSTM cell is its hidden state (like RNN), the current input (like RNN), and additionally the cell state. The output is similar in form: a new cell state and a new hidden state to be passed as input to the next LSTM cell.

So LSTM and RNN are similar in that they are repeated, they form a loop, they process a sequence. But the content of the sequence, the structure inside each cell, is completely different. Here we have tanh. The output of tanh ranges from minus one to one, so tanh is used to add information. Instead of having a simple tanh inside each RNN cell, we have this richer structure inside the LSTM.

That is the difference between RNN and LSTM. But at the same time, LSTM comes with more computational cost. The choice between RNN and LSTM depends on your case.

In natural language processing, when we have a sequence like this, we input a single element, process it using RNN or LSTM (it's your choice), and get one output. This is one type of sequence model, where we have an input and we generate just one output. An example is image classification: when you enter an image, you get one output, the classification of the image.

We have a different type of model where we have one input and generate a sequence of many outputs. We call this a one-to-many model. An example is image captioning, where you feed an image and the output is text that describes the image. I think you have this application in machine vision, where you feed an RNN or CNN with an image and then automatically generate a description of it.

Then we have another model, where you enter a sequence of data and the output is a single value. This is many-to-one. An example is sentiment analysis, where you feed the model a sentence and the output is positive, negative, or neutral. Image classification and text classification also follow this pattern. When you have a sequence representing your sentence or document, the output is one classification label, just one label.

And we have many-to-many. You apply this in advanced machine learning, when you have stock market data or time series data. In a time series, you enter a series of data and expect the output to also be a series of data. Forecasting for a year, forecasting for a month, depending on the time span of your data.

Another many-to-many example, and the best example, is translation or summarization, where the input is a long sequence and the output is another sequence.

These are the different types. We'll focus on the most important ones in NLP. I just talked about many-to-one, where you feed your model with a sequence and expect a single output, true or false, a label, positive or negative.

Now, we have RNN as a sequence model and LSTM as a sequence model. How can we solve the many-to-one problem using these sequence models? Previously, we used RNN as a language model, where we have a word and predict the next word. But here, we will try to use RNN or LSTM in a different way, where we take a sentence and predict just one answer: one classification for sentiment analysis, document classification, or language detection.

When we try to use RNN in such tasks, we face a very important problem. What is the problem? Look at this sentence: "The movie was terribly exciting." In RNN, we process a sentence from left to right. While we are processing a word, we don't have any information about what comes next, no information about any future word. This is the nature of RNN or LSTM sequences. You start from left to right and process one word at a time. So first we process "the", then "movie", then "was", and at the end "exciting." When we reach "exciting," we don't have any information about any future word.

For this type of NLP problem, we need to understand the whole sentence. Because a literal, word-by-word interpretation can mislead you. At the point where we've only seen "The movie was terribly," the sentiment looks negative. After we process "exciting," now we have the whole context. So RNN fails to solve this type of problem. Sentiment analysis and text classification require understanding the full sentence.

Researchers thought about improving LSTM to give it the chance to look at both the previous and future information. They named this architecture Bi-LSTM. This is an example showing the issue with using plain RNN or LSTM to model this sentence. For each word we have a hidden vector, and we have information from the previous word but not from the future word. The generated output vector is based only on past context. This can mislead your decision. When we reach "exciting," even though we represent the whole sentence as one vector, it may be classified as neutral, because "exciting" is balanced against "terribly," and the sentence ends up labeled as neutral.

So with bidirection, we make two passes. In the forward path, we have a forward RNN, and we also have a backward RNN. Two directions. We can run through the words from beginning to end with a separate neural network, with a separate embedding vector or a separate hidden state. For the forward pass, we create a specific hidden state, and for the backward pass, we create another hidden state, and the result is a concatenation. Now we can have a complete understanding of the context. We can look forward and backward at the same time. This helps when using RNN or LSTM for NLP classification or sentiment analysis.

So as you see here, we have two different sets of weights. Forward weights for input in the forward direction, and separate weights for the backward direction. We have a hidden state for the forward pass and a completely different hidden state for the backward pass. Then y-hat is a combination, at the end, a concatenation of the forward and backward states.

How does bidirectional change things? The idea is: you feed your sentence in. In English, this is the sentence. How do you feed it in? You feed your model the first word, then the second word. In Arabic, for example, Arabic reads right to left. So again, I feed it the first word, but I just proceed in the opposite direction.

The important idea here is the sequential nature of this architecture. I cannot feed the model the whole sentence at once. There is no parallelism here. The model has to wait. I cannot process x1 until I process x0. I have to wait for all the hidden output from x0. This is one of the limitations, which we will discuss later. Step by step, there is no parallelism at all. We feed the model the first word and wait until the model processes it, then we feed it the second word, and so on.

From right to left or from left to right, it doesn't matter. The idea of bidirectional is that we have a more comprehensive understanding of the whole sentence. We scan the sentence from left to right and from right to left, and the result is a concatenation.

This will reduce the vanishing problem related to RNN. Even LSTM is not very good at dealing with long sentences. LSTM improved things with the cell state, but it still suffers from the vanishing problem, because the vanishing problem is in the nature of any neural network training.

This is largely improved now, and we can use RNN and LSTM on a different problem: classification and sentiment analysis. The nature of classification and sentiment analysis requires a comprehensive understanding of the whole sentence, not just word by word. This is what bidirectional LSTM gave us.

Now the final hidden state is a combination of both directions, and the vanishing problem is reduced. Why? Because now the model learns from both directions. On the backward path, the learning rate might be very small, but we can compensate for this because learning is also coming from the other direction.

We can now use LSTM and RNN in a different NLP class besides language modeling. Using bidirection, we can now use it to solve many-to-one problems. This is a huge improvement. At each timestamp, y-hat, the predicted y, is a combination of the forward hidden state and the backward hidden state, forward plus backward.

Isn't it going to take more computational power and make the model slow?

As long as you make your structure more complex, of course, computation cost increases. It depends on your task and the nature of your task. If accuracy is a big issue, computational power is not a concern in this case.

Choosing between the different architectures: I did a lot of research in this field, and to be honest, it's trial and error. It's good to know when to use each architecture, what the significance of each architecture is, what its main characteristics are. But ultimately it's trial and error. You run your data with a specific architecture. If the performance is low, you try another architecture. This is what I did in many research projects. Once I found the architecture that best fit my data, I went back and wrote my justification. There are a lot of details related to each architecture, and your data may be different. So we try, trial and error with hyperparameters; change hyperparameters, change architecture. When you get the best result, go back and explain why this model gave you the best result, and write your justification.

In the final stage, as I mentioned, it may be RNN or it may be LSTM, but the idea is: we have a forward hidden state and a backward hidden state, and the final hidden state is a concatenation of the two.

I tried Bi-LSTM on a very small dataset for a sentiment analysis task. I have my text and my label, and I built a model on this very small dataset. It looks long, but the idea is the same.

The first step is tokenization, as usual, along with cleaning the data, the whole pre-processing step. Then we compute the maximum sentence length, which is a requirement for LSTM and RNN. You can compute it from your data, or you can decide on a fixed value for the maximum sentence length. Any sentence longer than that, you ignore the rest. Any sentence shorter than that, you pad.

Then you choose your padding technique. Padding post means I fill the rest of the maximum sentence with zeros at the end. Sometimes you choose padding pre, where you put the zeros at the beginning. This is very important. Compute the maximum length based on your data, or set it as a parameter at the beginning: decide the maximum length is 20, 30, or whatever, and specify it.

This is now my array. It's a one-hot encoding based on the index of each word in your corpus. When I try to test anything, and I expect you to do the same, verify each step. Don't apply the method without verification. Think about what you expect from this step, then verify to make sure you are on the right track.

This is actually very important when you use the embedding. When you create your embedding layer using GloVe or word2vec or anything else, test it. You have a common function that creates a dictionary and the embeddings, and pulls this information from GloVe. Make sure it's correct. If you have a word, extract the vector, and run a similarity check for this word against a very similar word to see whether they are distinguishable.

I computed the maximum length here, which is seven, and the vocabulary size in my corpus is 18. This is very important because we will try an embedding layer. The embedding layer depends on the vocabulary size and the output dimension you decided on.

My dimensionality is 16. Each word will be represented as a vector of 16 dimensions. To compute the dimensionality of the embedding layer, you have to identify the maximum vocabulary size. Sometimes it's not easy to identify your vocabulary size if you're dealing with a new generator, but this is one of the hyperparameters for LSTM. You have to decide your vocabulary size. Don't make it dynamic, because if you make it dynamic, the model doesn't have a fixed view of it.

In GloVe, we have different dimensions to choose from: 50, 100, and 300, and you decide which one. Here, I will not import the embedding from any pre-trained model, so the embedding layer will be trainable. The model will create and update the embedding layer during the training process.

Here I started to create the model. The model is sequential. The first layer is an embedding layer. Then I have a bidirectional LSTM, and you decide how many cells to use. Then we have the last layer, which is a sigmoid, because we need only one output. Then we compile the model, and this is the summary. Here, seven is related to the maximum length of your sentence, and 16 is your dimension. For the bidirectional layer, it's 64 as I decided, and then you have a dense layer with only one unit.

So this is the summary. Then I tested my model. You have to perform the same process on the test data as you did during training: tokenize it, and also pad it to fit as input to the model.

Then I wanted to make sure that my model actually trained the embedding vectors. I extract the embedding layer and check the first value. Here are the embedding vectors for "love," "movie," and "life," to make sure that I trained my text.

The dimension of the embedding matrix is 18 by 16. This matches my parameters: the vocabulary size was 18 and the dimension I chose was 16. These are the kinds of checkpoints you have to add to your code to make sure that everything works as you expect.

Here I have a somewhat larger dataset, a CSV, and this is how you can download it. Then I try to do a classification sentiment analysis task.

As usual, explore your data first. This is the first step in machine learning: explore your data, check for missing data, missing labels. The most important thing in exploring your data here is to check whether the data is biased with respect to the label. So I try to figure out how many of each label there are: how many true and how many false. This is very important. In machine learning, you need a balanced dataset for training.

I decided that the vocabulary size is 5,000, because I don't know the actual vocabulary size of this text file. This is one of the hyperparameters, and you have to decide it.

Pre-processing, then I have an embedding representation. This time I applied pre-padding, so you can see the zero values before the actual values. You can use pre or post padding.

My embedding dimension is now 40. I have an embedding layer, a bidirectional LSTM, and then a dense layer. Then you can apply classification, compute training accuracy, check the prediction, and print the confusion matrix.

This is how you use Bi-LSTM. As you see, the model is very simple, but you have to build your model in a sequential way to predict the label. You can use the same approach for any classification task or sentiment analysis task. Both work with bidirectional LSTM.

When do we use a single forward LSTM or RNN versus Bi-LSTM? The problem with Bi-LSTM is that you need access to the whole sentence during testing. It will not work with a real-time application, a live stream, because with a live stream you don't have access to the whole sentence. So for a live stream, a real-time application cannot use Bi-LSTM. Use Bi-LSTM when you have full access to the sentence during testing, not just during training. During testing, you have to have full access to the whole sentence.

And so Bi-LSTM does not work for language modeling anymore. Bi-LSTM is not a good model for language modeling. Because in a language model, we have a sentence or a word and we try to predict the next word in the context. We don't have full access during inference. During training, we have full access, but during testing, we don't.

Autocompletion, when you write a message, will not work with Bi-LSTM. Bi-LSTM cannot model the autocompletion process. We don't have a choice; we only use a single LSTM or a single RNN.

This also comes with more computational power. Instead of a single pass, we have two passes. Instead of one hidden state, we have two hidden states. Computing the average or concatenating the two hidden states takes computational power. The model becomes more complex, so it takes more time during training and also during testing.

That gives you an example of the LSTM in practice. Then I have my architecture, and it's up to you to decide how many hidden units to use. Don't use a huge number, otherwise your model will suffer from overfitting. Don't use too small a number, or the performance will be affected. Again, this is a hyperparameter: how many hidden units in your architecture.

Building on top of LSTM, researchers tried to find a solution for a complex structure or a complex sentence. When you have a complex sentence and you want to understand its context, they argued that a single forward pass through this sentence will not capture the complexity or the actual structure of the text. So they thought it would improve performance to use a multi-layer LSTM. Sometimes we call it stacked LSTM.

So this is the first layer, and its output is the hidden state, which becomes the input for the next layer. For the first layer, what is the input? It's a word. The input to each cell here is a word at the current timestamp plus the previous hidden state. For each timestamp in this layer, the input is the current word plus the previous hidden state. In the case of LSTM, also the cell state. Then the hidden state of this layer is considered the input for the second layer.

We try to deepen our understanding of the content of the text. We update the hidden layer with a second layer, where we can work with the multi-layer structure in your text. This improved many of the many-to-one classification and sentiment analysis tasks. The words feed into layer one, and the hidden state of layer one becomes the input for the second layer. This improved classification tasks and sentiment analysis.

But it comes with more training and more parameters. It has a huge number of parameters. The number of layers you choose for your model is one of the challenges. If you choose a huge number of layers, you make your model suffer from overfitting. Choose too few layers, and you may not capture the complex structure of your text. So it's one of the hyperparameters. Again, trial and error.

Another factor is the nature of your task. It's based on the criticality of your task, the criticality of the output. It's a trade-off between computation cost and accuracy. But this is one of the improvements they introduced to RNN-based sequence deep neural network models.

As you can see here in more detail, this is an X node, your input at a timestamp. If you use an RNN, the input is the previous hidden state and the output from this timestamp, computed vertically for each word. The last hidden state becomes the final hidden state, for the second layer, and so on. Again, what is the problem? More computation. And the main problem here is the sequential nature of the process. I cannot process x2 until I process x1. No parallelism. And high computation cost.

Then we have the last type here, many-to-many. A many-to-many task in NLP is one where we have an input sequence and expect another sequence as output. The main example is translation. You feed your model with a sentence in a specific language and expect the output to be a translation of that sentence into another language. Or summarization, where you enter your text and the output is a summary of it. How can we use RNN and LSTM for this type of NLP task?

They came up with a new technique called sequence-to-sequence, seq2seq. This is actually part of your transformer; the transformer is built on this concept.

So what is a sequence-to-sequence technique? We enter a sequence and we expect an output, but the important part here is varying length. In translation, when you translate between one language and another, is the number of words in the source language equal to the number of words in the target? No. In summarization, the number of input words is not equal to the number of output words.

Sequence-to-sequence is very good at dealing with variable-length input. I don't need to pad in this case. They have a technique to deal with variable-length input, which is the main architecture we use to implement sequence-to-sequence. We call it encoder-decoder.

The architecture consists of two different components. The first component is the encoder, which we feed with, for example in translation, our English source sentence. After processing the input, the encoder passes its output to the decoder. The decoder takes the input from the encoder and begins to translate, word by word.

This is the basic architecture for encoder-decoder. I have an encoder for processing the input, and one output from this encoder is used as input for the decoder, which starts to process and generate the translation in a different language or generate a summary.

Inside the encoder, we can use LSTM or RNN; it depends on your task. The most important part here is the output of the encoder: one fixed-length vector, and it is fixed. So we encode all the information in the input into this fixed-length vector, which is passed as input to the decoder.

The decoder takes this information, and based on this information and the current word at each timestamp, it tries to predict the next word. It's like a language model. The information comes from the encoder, and at each state I try to predict the next word.

So we consider the translation problem as a conditional language model, where based on the input we try to predict the output. If you try to predict the probability of y given x, it's the probability of y1 given x, the probability of y2 given y1 and x, the probability of y3 given y1, y2, and x, and so on. What is the x in this case? The fixed encoder output. So we treat this as a language model, and we can compute the probability and train our model based on this formulation.

This is what happens inside the encoder-decoder. The encoder here, the source is in English or French, whatever your source language is. I feed the encoder with the source using RNN or LSTM, and the final hidden state is the one fixed vector, and we feed the decoder based on that. With the current hidden state, and what comes in from the encoder, I try to compute the probability over the vocabulary.

This happens during training. There is a difference between training and testing. Can you imagine how we train the translation model? What do we keep? Pairs: one sentence in English, and its equivalent in French. What the model tries to do during training is find the alignment between the source and the target. This is the training objective.

When you train the translation model, you train it with a huge number of pairs: a sentence in English and a sentence in French, or any other language. During training, we feed the model with the first sentence, and based on the output we compute the probability and the loss at each stage. The total loss at the end is the average of all the losses. As you see here, this is the total loss. We use back propagation end-to-end during training. This is how the model finds the alignment between the source language and the target language.

During testing, we don't have access to the target. We have access to the source. You feed the model with the English text and ask the model to predict. Again, it's like a language model. Here, I compress all the information from the encoder into the last hidden state. The last hidden state is passed as input to the decoder. At each timestamp in the decoder, we use the previous hidden state and try to predict the next word. For example, when you predict the next word, it's predicted based on the previous hidden state. Language model. This is how we train and test translation using RNN or LSTM.

What is the problem with this model? It's a common problem, a bottleneck. What do we mean by bottleneck? We compress all the information coming from the encoder into one fixed-length vector, which we pass as input to the decoder. The sentence may be short or long. How can we compress all the information into just one fixed vector? The decoder doesn't have any access to the intermediate encoder hidden states, only to this last one. This is what we call the encoder-decoder architecture, and this is its main issue.

The main problem with encoder-decoder is the fixed-size hidden state. It's like reading a book and then being asked to summarize it in one paragraph, and then being asked to recreate the book from that paragraph. It's the same kind of problem. How can we compress all this information into such a small representation? This is what we call a bottleneck. This is one of the main problems with using RNN or LSTM for summarization or translation in NLP.

Another problem here, as mentioned before, is the sequential nature of RNN or LSTM. No parallelization. I cannot apply any parallel processing. I have to wait, then feed the output to the decoder. The sequential nature is a big issue.

Based on this, the bottleneck problem and the sequential nature of RNN or LSTM, where we have to process any sentence one word at a time, they came up with another solution. It's called attention.

What is the idea of attention? If you're away from college for one or two weeks and then you come back, you find out what's required of you. The next day, you have a midterm, a quiz, and an assignment. What do you do in this case? Focus on the one with the most weight. Set your priorities. Which one is most valuable? If the midterm is 20 percent, the quiz is 1 percent, and the assignment is 5 percent, which one do you focus on?

Give priority to some tasks. Give a weight to each task. The task with the highest weight is the highest priority. This is the idea of attention.

The other problem with sequence-to-sequence is that each word in the sentence is treated as equally important. In RNN and LSTM, each word has the same importance, while in reality some words may be more important than others. This is the nature of any sentence in any natural language. Some words are highly important for creating the context or helping you understand the sentence, and some are less important. For example, "that" is not important. "I" is not very important. "Love" is important, but less than some other words.

So how do we come up with an idea to give different weight to each input based on how important it is to the translation output or the summarization? What is the keyword in your input sequence? This is the idea of attention.

The idea of attention is something like this. We have different words at each timestep, T, T1, T2, T3, and for each word we have a vector: V1, V, and so on. This is your input, and we have the output, Y1, Y2, Y3, Y4. What we need to do is figure out how to assign a different weight to each word based on its importance.

You remember similarity measurements. For example, in a two-dimensional space, you have a vector that represents one word, V1. If another vector is similar to it, we expect the output to reflect that. If the two vectors are not similar, the output reflects that too.

At each step in the decoder, we now have access to all the inputs, and we can compute the weights across them. This is the idea of the attention mechanism. Attention is a weighted average over the set of inputs. We compute the weights at the decoder by computing the pairwise similarity between each encoder hidden state and each decoder hidden state.

Now, the decoder hidden state has access to each element inside the input, not only to the fixed-length bottleneck. Now it has direct access, and we can compute the weighted average, transform this weighted average into a probability, and figure out which word has higher probability than the others at this timestamp. This is the step introduced by attention. The benefits: variable input length and long-range dependencies. The whole idea is that at each timestamp the decoder has a direct connection to the encoder at each timestamp.

This is represented like this. This is the encoder, where you feed your source, for translation or something similar. Then at each timestep I can compute the dot product between the decoder hidden state and each encoder hidden state, compute the weighted average, and produce what we call the attention score. Dot product at each hidden step. Then we use softmax to turn it into a probability, and we figure out that at this timestamp this word has higher priority or higher weight than the others, which tells us which translation to use.

Sorry, so far in the labs we've tried to remove stop words and punctuation like commas, periods, and full stops. For translation, should we keep them?

Yes, for translation, for sure. Stop words are very important in translation. Full stops and commas too. For translation, we have to keep everything.

I was going to say, for example, the word "was" then "I," or not. Translating into a language where "I" is gender-specific, or where "was" indicates the past in a different grammatical tense, every word is important.

I agree with you. Every word has importance. I don't mean we should ignore any word. But every word has a different weight. We're not ignoring it, because the performance of the translation has to reflect your input sentence, with all the grammar and syntax. If the translator outputs another sentence that includes all this information, that's how we measure the quality of the translation.

You can notice the performance of translators now compared to 10 years ago. There's a huge difference. That's because there's been improvement in the structure, improvement in the architecture, and a big step in improvement came in 2017 with the transformer. All these steps led us, step by step, to how the transformer idea came up, in the research published by Google. They didn't invent it in a week. They built this knowledge on all the previous trials, all the previous structures, and then improved performance on top of that. So the transformer builds on top of previous work, not from scratch.

This happens at each step in the decoder. To compute the prediction of y-hat, the last hidden state is the input for the next step, using the decoder hidden state and the context vector (the weighted average) from the encoder. Then we can use it to predict y-hat at each state. We repeat the process for each timestep. For y2, we go through the same process: we have access to all the previous encoder hidden states, compute the weighted average, and use it as input to produce the next word in the output.

Attention is very important. It came up with a brilliant idea to improve the performance of tasks like translation and summarization. Did it solve all the problems? We're still far from perfect on such tasks.

So attention has benefits and challenges. The benefits: we can handle variable lengths. We can now have a deeper understanding of the context. We don't treat each word as independent or give each word the same weight in producing the output. But we still have some challenges.

What are the main challenges? We haven't fixed the parallelism problem. Sequential processing is still there. We still have the sequential problem. We need to improve the computation. We can't wait for the model to process one word, one timestamp, before moving on to use the output as input for the next state. Sequential processing.

Another problem: is the position of the word represented in any way in this system? How do we represent the position of the word? There is no information about the position of the word, no way to overcome that within a purely sequential system.

Then in 2017, a group of researchers at Google published the paper "Attention Is All You Need." They introduced attention, the same idea of attention, but they improved it in two different ways. They improved it with self-attention, and they improved it with multi-head attention.

They kept the idea of attention, which uses a dot product to compute the importance of each word, but they improved it by introducing what we call self-attention and multi-head attention. So the transformer is a sequence-to-sequence model with self-attention.

This is the architecture of the transformer. You have the input, and then the transformer produces the output. What's inside this block? An encoder-decoder. The same idea, but with more detail and more improvements. What is the architecture of the encoder? It's different from the previous encoder in the sequence-to-sequence model. And the architecture of the decoder? It's very similar to the encoder, with a small difference.

They use the same idea. We have an encoder and a decoder, and there's a connection between them. And they handle parallelism. Now we can have some level of parallelism in the process of dealing with the encoder, using self-attention or multi-head attention techniques.

I don't want to start the encoder-decoder now because the encoder architecture is very complicated and has a lot of details. One of the important details is that the input to the encoder is not just a word, but a vector representing this word. They add more information about the position of this word. They have very intelligent techniques to encode the position of each word. So the vector is not just the embedding vector anymore; it's the embedding vector plus a position vector. Now I feed the encoder with two important pieces of information: the embedding vector plus the positional encoding of this word. So they solved the problem of position.

We can now have information about the position of each word, because position is very important in any language. Position affects the sentence structure, the grammar. They feed this combined information as input to the encoder.

The input is an embedding vector using the previous embedding vector technique. Then we add positional encoding using sine and cosine. It's a very intelligent process for how to add position information to the vector. Then they feed it into the decoder.

The decoder consists of six different layers. For the output of the encoder, the decoder has access to the output from each layer of the encoder.

The architecture is very complicated. I hope that next lecture I can finish it all, but I can't; I have to divide it into two lectures. Next lecture we go into the encoder. Once we understand the structure of the encoder, I will go through it layer by layer and explain what happens in each layer, and then the encoder-decoder as a whole. The encoder is very similar to the decoder, with just a little difference in the top layer. The last layer of the decoder is the only difference between encoder and decoder.

The encoder-decoder sequence is how we use RNN and LSTM to handle different tasks in NLP.

Let me show you an example. I created a translator to show you how translation happens. This is a translation with attention.

During training, I have sample data, as you saw, a pair of sentences for any translation task. I have some sentences in English and some sentences in French, and I add start and end tokens to mark the beginning and end of each sentence.

Tokenization, embedding, and here I use GloVe as my embedding. So I will not train the model to create the embedding vectors; I just use GloVe. This is a common function with GloVe. I create a dictionary where each word has an index, and I use this index to access the vector for the word.

This is the encoder. This is the structure of the encoder. I tried to put some key comments to explain what's going on. The embedding dimension here is 50, based on GloVe which has 50-dimensional vectors. 128 is the size of the hidden state.

In LSTM, remember we have a hidden state and a cell memory, which we call the cell state. This is one of the hyperparameters you have to specify when using LSTM: the dimension of the hidden state.

Then we have the encoder inputs. It's an embedding layer that comes from GloVe, with trainable set to false. What does trainable false mean? This layer will not be part of training. It's a frozen layer. I don't want to train it; I already have the vectors from the pre-trained model. If you create an embedding layer from any pre-trained model, you have to add this.

Trainable true if you want to train it, false if you don't want to. Trainable here is false. By default, trainable is false. Be careful, because trainable by default is false. We will not train this layer. But if you want to train the embedding on your own data, it should be specified as true.

In the encoder, I have an LSTM with return state set to true. What is return state? I want to return the output for each state. Why? Because the decoder will use it. So I want to output the hidden state for each step.

The output here of the encoder state, this is not with attention, by the way. It's the combination of state H and state C. What are state H and state C? This is without attention. This is the basic encoder-decoder.
