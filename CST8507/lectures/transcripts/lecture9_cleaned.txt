Let's go through the self-attention, which is the same as the one we covered. So we have V1 embedding for word 1, V2, V3, and V4. This is the embedding for each word.

So let us say we need to compute the self-attention of word V3. What we can do here is perform a dot product with V3 and the previous vectors.

Now, after the dot product, what can we get? Scores. So here we have s1, s2, s3, s4, the result of the dot product.

Now, we perform a normalization to make sure that they are in the same range. After normalization, what is going to be the output? Some weights. So this would be 3,1. This would be 3,2. That would be 3,3. And that would be 3,4. Because now I want to check the self-attention of the word V3.

Now, after we got the weights, we can now multiply again by V's. So I will write the V again here: V1, V2, V3, V4. And then we multiply this by this, and this by this, and we get the output. This is the same equation that we have.

The same process, the same attention. I try to compute the self-attention of V3. My sentence consists of four words. I apply the dot product of V3 with each one. I get some scores, which are attention scores. I normalize these attention scores to get some weights. Based on these weights, I can multiply by the original V. And then I have the weighted sum.

So this is the self-attention. What is the problem with this technique? Is there any training here? Any neural network? No. There is no training at all.

So to make this process trainable, we just multiply each vector by a matrix. A weight matrix will be trained during the training process. They borrow some analogies from databases. So they consider this word as a query, and the original word they consider as keys, and this will be the values. When you have a query and you search about this query, you search in your keys, and then you get the values from the database. So they just borrow the analogy from there.

Then they decided to have a matrix here. We call it MQ, and a matrix here, MK, and we create a matrix here too. And these will be trained during the neural network training. So we make this process, or the transformer, trainable. So it's not a basic calculation, but something that we can learn during the process.

All right, so I will redraw it in the form of a neural network. This is the input of the neural network, V1 through V4, they are vectors for each input.

What is the output of the multiplication? Scores, perfect. So we have S_i,j scores. What is the next step? Normalization. So we can normalize. What is the output of the normalization? Weights. So the weights will be W_i,j. And then what is the next step? Multiplying the values by the output of the normalization. Here I'm multiplying. And what is the output of this step? y_1 hat, y_2 hat, y_4 hat. The same.

And we call this block self-attention. Back propagation comes to update these points. This is a neural network of the self-attention mechanism where we introduce training.

Now, they thought about the multi-head attention mechanism. What is the idea of multi-head attention? As we saw in the previous example, and the example I wrote on the board, there are multiple attentions for the same word. So the idea comes from the machine vision course.

Why do we have layers in a convolutional neural network? Is that a feature? Feature in the image? Yes, it's a feature, but why should we have layers? Abstraction, layer abstraction. So it doesn't just take the pixel shapes and colors, then it detects edges, and then it becomes more object level. OK, got it. So I want another answer. Why should we have layers? In any image, it includes different details, yes? One layer may detect edges. Another layer may detect other details. So this is the same idea.

With multi-head attention, we apply it to the same sentence to access different or complex contexts. Different layers of attention. So they just updated the same design with a very simple update. So instead of having one layer here, I can have multiple layers. And they name them 1, 2, up to H. So this is head number 1, head H. And the same for here, and the same for here. But the output will now be different.

So instead of having just S_i,j, now I have multiple layers. So I have S_i,j for head 1 and S_i,j for head H, where H is the number of heads. And the same for the weights. I have multiple weights here. So W_i,j on the first head and W_i,j on head number H. And then also here, we will not have one output vector. We will have different output vectors based on the number of heads. So Y_1 hat for head 1, and Y_1 hat for head H.

And the input is a query, right? So it's attention. So the self-attention that we have, for example, V1 multiplied by V1 is weight 1,1, V1 multiplied by V2 is weight 1,2, and so on. And then we can compute Y_1 hat for each one. So we can compute the weighted sum, and that's it. This is the matrix for the attention heads. And y_2 hat is the contextual embedding for V2. And this is the idea of multi-head attention used in the transformer.

So the transformer, as I mentioned before, was published in 2017, and the title is "Attention is All You Need." You don't need anything other than attention. The transformer paper, based on the training data, the objective of training is translation. So if you read the paper, the data is in pair form, it's a full translation process. They trained the model for translation tasks. And it's a huge model. It takes about 3.5 days to train on two types of data using 8 GPUs. The first one is from English to German, and the second one from English to French. The first dataset was from English to German, and I think it is about 4.5 million pairs of sentences. Remember how we train translation? We feed the model with a source and the target language. And the other one, from English to French, I think it's 36 million pairs of sentences. This is how they trained a transformer. And the people are from the Google Research Group.

All right, so what is a transformer? It came to solve sequence to sequence tasks, like translation. We feed the model with a sequence, and the output of this model is another sequence. And it relies on self-attention to compute representation for the input and also the output.

Now, how can we do this? This is the transformer architecture. It is based on the encoder decoder. But they combine the encoder decoder into one unit. We call this unit the transformer. So we feed the input, which is a source language, and then the architecture will output the translated target language.

And this is the actual architecture of the encoder and decoder. From a high-level point of view, this part is encoder, and this part is decoder. You can find here Nx. It means we have different encoders stacked on top of each other, the same idea like convolution, encoder, and decoder. So it is not one encoder. It is multiple encoders. And N, based on the paper, equals six. So we have six encoders, and we also have six decoders. The architecture is the same for all the encoders, and the same for all the decoders.

We feed the input to the first layer. So the input here goes to the first encoder layer. And then as you see, we have a multi-head attention that we just computed here, the same technique. Then an Add and Norm block, and then a feed forward, and then an Add and Norm block. I will explain this in detail.

The input will be embedded, and then we add very important information, what we call positional encoding. Why do we need positional encoding? Because in RNN, if you remember, we feed the model one by one, token by token. So we know the order. But now in the transformer, we feed all the input at once. So we need to preserve the order of each word because in contextual information, the order is very important. That's why they add to the embedding what we call positional encoding. I will explain how they did it.

But this is the general idea. Feed the model with an embedding vector for the input words, for the input source language, add positional encoding information to the same vector. And this is added only once. And then it goes to the multi-head attention layer, just to compute it. And then we have Add and Norm, and then feed forward neural network. Here there are six layers.

The output of the last layer will be the input for the decoder. And the decoder, based on the input from the encoder and what it has so far, tries to predict the next word. They are very similar in architecture. We have one exception here. Look at the two architectures, what is the difference? Only one difference. So they are similar in everything except masked multi-head attention. The decoder uses an extra layer to compute masked multi-head attention. And this is a general view.

Now we will go deep into each one. I will start with the encoder, then when we finish the encoder, the decoder is the same, but I will explain what we mean by masked multi-head attention. So this is the anatomy of the encoder. Again, this is one layer, but the six layers are the same to have access to multiple levels of information, contextual information, complex information from the same word. And this number is a hyperparameter in the transformer. In the original paper, it was six. In other implementations, it comes to different values. We have six layers in the encoder, six layers in the decoder.

As I mentioned here, we feed the sentence at once. Why? We need parallelism. Remember, what is the drawback of the previous techniques using deep neural networks, RNN or LSTM? We feed the model one by one, process word by word, you have to wait for the output of the previous one. But they try to overcome this problem so they feed the model with the whole input at once.

And then this creates an embedding. The first step is tokenization, and they use specific techniques for it. It's byte pair encoding. I gave you a link to understand how they perform this tokenization. After tokenization, we create an embedding vector for each token, and the length of this embedding is 512. Then, to this vector, we add information related to the position, and then feed it to the first layer which is the multi-head attention layer.

Now the question is how can we add positional input for each word? If you feed all the words at once and you have parallel processing for each token, separate from the other tokens, we lose very important information related to position. Why is position very important? Now, we started to think about a technique to add information about position to the embedding vector.

One of the thoughts is, we can do something like that. If I have the vector for this word of length 512, and we have the embedding values here, I can add another vector with the same length. I can create a 512 dimensional vector and each value here will be based on position. What is the problem with this technique? Imagine that the word is in position 100 or 50, so what will happen here? We will lose the most important information in the embedding, which is measuring similarity. Now I cannot measure similarity; the data will be completely changed. So this technique is not the one. But this is one of the ways that we add something that should be unique. The position should be unique for each word. And we think about this technique, this technique is unique, but we have a big problem here because the values will be changing, and then we are not able to measure similarity, the similarity will be lost.

We think about using a function. We can use a function to create positional encoding and we keep the same idea. We will generate a vector of the same length, 512, and this vector will be added to the embedding. But how can we put the values in this vector? We initially think about the function. What about if we use sigmoid function? And for each position, for example number two, the projection of this position in this function is considered as the position of this word. There is a problem here. The nature of the sigmoid function is that at some point we have the same values, while the position should be unique. You cannot repeat it.

Then, in the transformer paper, they thought about using sine and cosine functions. Not only sine, both sine and cosine, to give you more variety of values. And they have a very interesting equation for this. So the position value for each position, this is the position of the word, it's divided into two cases: if the index is even, then we use sine; if the index is odd, we use cosine. This gives you a different variation with different wavelengths. And they built their equation dividing by 10,000. I actually tried to ask and search why it is 10,000. The answer is that 10,000 gives you a different range of the frequency in the sine and cosine. So if this number is 10,000, then you have the range from 2 pi to 2 pi multiplied by 10,000 for any dimension of your vector. And then as you see here for different indices, look at this position, the projection of this position in a different wavelength of the sine and cosine, this will be your vector.

This is an example for a simple case, if you have a dimension of 4, only 4. And this is how we can use the equation. I'm not confident about this equation, so to let you understand, I created an Excel sheet to compute it step by step to give you an understanding of how we can compute this. So for example, for the word "I" in a sentence, "I" is in position zero. Then is zero even or odd? Even. So do we use sine or cosine? Sine. So just put i for the position. It's 0 here, and i equals 1. Because here I need a dimension of the vector, in this case, 4. So here this is i equals 0, i equals 1, i equals 2, i equals 3. Then we compute the first positional embedding for position 0 at index 0, and this is the value. And then for index 1, 1 is odd, so we use cosine, and then we compute the value for each word. Is the computation clear?

This is how they compute positional encoding using sine and cosine. So if you understand this for a small dimension, they repeat it for 512 dimensions. Yes? Why do we say d equals 4? For simplification only, just to show you how the calculation happens. This is a very limited example for simplification, but actually d in the transformer equals 512.

So back to this, this is what happens now. In this example, they compute the frequency as 100 to make it easier. Then they compute P1, P2, and so on. Now I have one vector that includes the embedded information plus positional information.

If I have a sentence with multiple I's in different positions, do they take different vectors? Yes, of course. And this is the idea of contextual information. The same word in a different position has a different impact. That's the idea. Each word has a different position. And for this word, the position is unique. So you cannot find two multiple values of this position. It's not duplicated. Why? Because we use the frequency of sine and cosine, and we add more range by using the difference between cosine and sine.

So this is how they add position to the embedding. So they combine both, and they feed it to the self-attention. What happens in the self-attention? We explained it here on the board. How we compute self-attention. We use the same technique. We have queries, keys, and values.

So this is the self-attention part. Again, we have three copies of the same embedding vector: Q, K, and V. And this is simplifying the process that I showed you on the board. So we have multiplication between K and Q, and then we scale it. Then I will skip masking now because masking is related to the decoder. I will apply softmax to normalize, and then we have a multiplication. Multiply again by V, the same, and this is a self-attention block.

Now, the same idea here: for each query, I have keys, and then I multiply the key to get the scores. Scores give me weights, then we can add attention value for each value. And this is what happens in a self-attention block. They compute, they make a self-attention computation in matrix form. So we put all the input embedding in matrix form. When we create a matrix, we can benefit from the GPU to do parallel computation. So here, this is an input and an embedding matrix. This is a matrix for Q for learning, and this is K, and this is V, and then we perform the same output.

So this happens after, in the pipeline, this happens after the generation of the first matrix, right? Yes. Here I add the position, and it is added only once. Then I feed these two combined to the self-attention layer. And in the self-attention layer, we now have scores or weights to predict what is y hat, which is a contextual representation of the input sentence.

It's a transformer. They didn't use a dot product, like we did here. We didn't use a dot product between Q and K. We used something we call scaled dot product. Why? Look at the softmax. And this is their motivation for that. If you have this vector, and you apply the softmax, this is it. After applying the softmax, this will be the value. Softmax is an exponential function. And this will be the power.

Now, if the value is bigger than this, we have big values. What about the softmax results? We have more and more big values. What is the softmax output? Very, very small for small inputs. This is one of the common features in the softmax function. Softmax function is very sensitive to big values. Now, how can we overcome this problem? Instead of performing what we call a dot product, we will do something we call scaled dot product. And we can find that the scale is the square root of d.

So for this example, if you have this value, which is big, and then we apply softmax, the softmax result here is very, very small. But if we scale these values by the square root of d, based on your dimension, and then apply softmax, we now have values in the softmax function. You have to explain why we have scaled dot product. So they explained this in their paper. We will not use the dot product because softmax is very sensitive to big values. And this proves by values that yes, softmax is very sensitive to the dot product. So that's why they thought of another equation. And you find the scaling factor is the square root of the dimension of the vector. So square root of 512. So if you divide each value by the square root of the dimension, and we apply softmax, we will have values here rather than zeros. This is why we have scaled dot product.

And this is empirical. I tried to prove it, actually. So I have here the original distribution of some data. And then apply softmax. This is the distribution of data after applying the softmax function. But if we scale the same data and apply softmax, you can now notice the difference.

So this is what happens in a multi-head self-attention layer. We input the query. Like here, the input "thinking machines." We get an embedding plus position. Then we divide each one into query, key, value. Since we have multi-head attention, how many heads in the transformer? We have 8 heads. And since we have 6 encoders, for each encoder we have 8 heads. So what they do is divide 512 by 8, the number of heads. So each part will be computed in parallel. So multiplication or self-attention for each token will have the same equation here. So this is the query, after dividing into the 8 heads, and then this is the key, and this is the value, and we use matrix multiplication because we have eight heads or eight head attention. And then we can compute K, compute V, and this is a summary of what happens actually inside the multi-head attention.

The same, so the input here is words, embedding plus position again. Then we have a query, we multiply the query by keys, and then we divide by 8, the square root of the scaling factor. Not dot product, again, this is a scaling factor. And then we apply softmax. And then we multiply the output of the softmax by V again. Then we get Z, which is y hat for the output of this multi-head attention. And you will find out for the input here, for this specific head. So this will be repeated for each head.

So I try to write here. This is the scaling factor, if you remember, the scaling factor to overcome the problem of the softmax being sensitive to high values. The dimension here is 64. Why? Because we divided the 512 into 8 heads. So each one would be 64. The square root of 64 is 8. That's why we scaled the query and the dot product by 8. The sequence is clear? Any questions?

So this is the output of each head. We're still in the multi-head attention block. And then this will be our input to the feed forward neural network. So back again to the architecture. After self-attention, the output of self-attention goes through Add and Norm. I will explain what we mean by Add and Norm in a few slides, but this will be an input for a feed forward neural network.

As I mentioned, the input is to a feed forward neural network. One of the things that I didn't explain here, after applying softmax, we multiply again by V, the original V. Let me explain this. This is an Add and Norm layer. And I will show you why we have to do this in each step.

So after applying the same process for eight heads, so this is the output for each head. Z0 denotes the first head, Z1 for the second head, and Z7 for the eighth head. To feed this to the neural network, the neural network expects to accept one matrix, not seven or eight matrices. So we need another layer here to compact all the attention heads. And then we multiply by another matrix that we learn for the attention. And this will be fed to the neural network.

Now, what is the feedforward layer? It is a two layer feedforward neural network, fully connected ReLU. So the key here, it is not a normal feedforward neural network. The feedforward neural network is called a position wise feedforward neural network. Why position wise? Because for each token, we compute all the previous processes in parallel. So it's related to the position for each token.

So this is the point here in the feedforward neural network. When we compute the self-attention, we have some important information, but what is this information? What each word should attend to. But do we use this information in learning the process? No. So for example, if I go through the steps, we have the input here. And then the input goes to multi-head self-attention. And from here, we get very important information. What is this information? For each word, what or who I should attend to. So when we come to this level, for this word, we know what to attend to. And then we go to Add and Norm, and again I will explain why we need Add and Norm. Then we get to feedforward neural network. And the feedforward neural network is responsible for this stage, which is, what I do, or what I can do, that I conclude from this. I learned attention here. Now, how can I use this attention? So what is the knowledge that I gain from this attention? And this is the role of the feedforward neural network. It works like memory storage or memory knowledge that we learn using this architecture.

So this is the idea of feedforward neural network. Again, it's two layers and it is fully connected ReLU. So if any value is less than 0, just make it 0. Any value greater than 0 will remain. So it's working like a filter. Because it's two layers, we have two learning matrices. From here, we have two W's, and this is what the feedforward neural network has.

Now, what is Add and Norm, and why do we need Add and Norm? I noted in the architecture of the layer something that I didn't explain. Right, so the input embedding, the attention, feedforward. But here we have Add and Norm. What will we add? The output of the multi-head attention with what? The input of the multi-head attention. And this is repeated here also. The output of feedforward neural network will be added to the input of the feedforward neural network. This is what they call residual connection.

So what is the idea of residual connection? If you remember in neural networks, in recurrent neural networks and LSTM, we suffer from a very important drawback, which is vanishing gradient. And here, the problem is maybe worse. It's a very deep architecture. So after we compute the self-attention, we get some information about the weights. It's a weight. Now, to not lose what we have, we need to add these weights to the original vectors. And this is what we use here, Add and Norm. So do not forget what we have. And avoid the vanishing problem, gradient vanishing problem. And this is also repeated here. Don't forget what we have. So instead of just taking the result, because it's a deep, deep network, and you know, back propagation is differentiation. And we saw how it can become zero. So this is a very deep neural network. So to keep this value and to keep learning, we have to add this in two positions, the previous. So here, this is the output, this is the input. The output of the layer is a combination of the output of the layer and the input of the layer. So to keep learning, not forget the previous information. Don't forget what we started with. And this is the idea of residual connection. Let us add and refine rather than replace. So we will not replace the initial vector with the refinement, but we will add this refinement to the initial vector. And that's why we have Add and Norm.

So this is what we have. And now I think we finished the encoder. The only thing that's left to interpret is Add and Norm, which is repeated here. We add that to the value vector and normalize the data to be fed to the next step.

The idea here, which is a big advantage in the transformer, is that everything runs in parallel. All the multi-head attentions work in parallel. We feed the sentence to all the layers at once. There is no sequential here. And then for each layer, as we mentioned, we have different Y's. At the end, we compact all these Y's. For each layer, we have only one output, but all the heads work at the same time. This is the output of the first layer in encoder, and then it will be input for the next encoder layer. We just use matrix multiplication, and GPUs work well with matrix multiplication. That's why parallelism here is a good advantage in the transformer.

Now back to encoder-decoder. Now we finished encoder. Everything is clear inside the encoder. Then the output of the last encoder will be input for each decoder. We have six decoders. This is what we have here. I will show you the details, how the output becomes the input, and in which way, but now we will focus on that in detail.

As I mentioned, it is the same. The only difference is here: masked multi-head attention. So what is the difference between multi-head attention and masked multi-head attention? We still have multi-head attention in the decoder, and we also have masked multi-head attention. But what is the difference? The difference is, while we have masked multi-head attention, during the training, we feed the encoder with the input sentence, and we feed the decoder with the target sentence. We want to compute the multi-head attention in the decoder with all the previous words, not the future words. Why? Because otherwise we create problems. We're not allowing cheating. If the decoder has access to the future words, if now I am in the encoder or through the decoder, I have this sentence in the network and this is a decoder. And then encoder, this is encoder. And this is the step. If the decoder can process this word, what does the decoder want to use? The decoder wants to use what it processed plus the source. So it has this information. That's why masked multi-head attention. So for any future word, just replace it with minus infinity. So the softmax later will convert it to 0. And this is the idea of masking. Any future word will be masked from the decoder to let the decoder learn, not cheat. And this is only the first step. So this is the same process as multi-head attention, except that we use masked multi-head attention using a lower triangular mask.

Now I will focus on this step. What is this step? Now we have the same process like here. Embedding, position, everything is the same. And then we feed the model, we perform the masked multi-head attention. We have Add and Norm to not forget what we have. And then the output goes to multi-head attention. Layer here, we have three inputs. What are the inputs? The input comes from the encoder. So in multi-head attention, we have three values: query, value, and keys. The query comes from the decoder. Keys and values come from the encoder. And this is logical, right? The query comes from here, so this is the question that the decoder asks. I want to learn this one, the situation that I have, plus the input from the encoder. So the keys and values come from the encoder, while the query comes from the decoder. I want to attend to what? And this is the idea of self-attention. To whom I want to attend. So I attend based on the values that I have so far, plus the source, so that I can predict what is next. This is the information that the decoder needs. And after that, everything will be the same.

So this is how the encoder-decoder stack works. I have multiple encoders. And the output of the last encoder, this is very important, will be passed to each layer of the decoder stack. So the process repeats. At the end, I have a softmax in the decoder. Why? We make a prediction. So we make a prediction layer. We predict what is the next word during training. So that's why at the top of the decoder, we have a softmax that tries to predict, to compute probability, according to all the input, of what word comes next.

All right, so the architecture of encoder and decoder is now clear. What is the difference? What is the importance of each part of it? So I tried to stay away from detailed equations. We will not create a new transformer architecture, but it is important to understand this architecture. So I tried to just focus on understanding the architecture.

Now, who knows Hugging Face? So what is Hugging Face? It's an ecosystem. You have all the models, you can just choose between them. Models, datasets, documentation, different organization techniques, different dataset techniques. You don't need to do anything, just choose. You have here a set of, I don't know if the interface is changing now, because I took the screenshot maybe two years ago. I need to check if the interface of Hugging Face has changed, but here we can check the task, and then we can see all the models related to the task, and Hugging Face gives you a model card and a dataset card, where it explains how this model was trained and all information about the model, and all information about the dataset. You don't need to train the model from scratch, you can just download the weights, and then fine tune the model based on your data. So, no need for any training, you don't have to spend 3.5 days to train the transformer from scratch, but you can use many, many models. And this is a very simple example for text classification. We use a very simple method here, a pipeline. Pipeline is just an abstract method, it encodes all subprocesses. It will load the model, compute the weights, use the weights, just the pipeline, and you give it the task. And then use it to predict, to perform this task. A very simple way to do any task using Hugging Face where we have all the models the same. But it is very important. My tip of advice, don't download any model unless you read the model card. Read the model card to understand which model you have to use. I am not asking you to train the model from scratch. You use a pre-trained model, but try to invest some time to understand how they trained this model, which source of data, what are the parameters, what is the size of these parameters? So there are a lot of considerations you have to take care of. And for any NLP task, named entity recognition, question answering, this is a very simple one. Summarization again.

And this is a tree, and this will be our task for the next lecture. After 2017, we have multiple forms of transformers. We have the original transformer which was encoder and decoder. Some other versions of the transformer, like T5, BART, all these use encoder and decoder. Some other models use only encoder. Other models use only decoder. And third, this will be our task for next lecture. It is built on only encoder. It takes the idea of encoder, improves it somehow, and then that's it. So BERT, another version of BERT, and all these versions, maybe there are some newer versions now, but they use encoder. Some other architectures use only decoder like GPT. GPT uses only the decoder version, not only the transformer, but it's a combination. I will talk about GPT later on, but this is the model. So some models use only encoder, some models build on only decoder, some models build on both encoder and decoder. So there are many variations here.

Right, so we come to the last part of this lecture. What are the challenges in transformer-based models? We still have some challenges, yeah. Every natural language processing task before transformer is completely different after the transformer. There is a huge improvement in the performance after 2017. But still, we have some challenges using these models.

The first one is language. Most of the models are built on one common language, which is English. So there are some trials with different languages, like Arabic, but still, most of the improvements and models are built on one specific language, which is English. So this is one of the challenges for transformers.

Data availability. We need huge data. And even if you use a pre-trained model, and you try to fine-tune, you need huge data. So you cannot fine-tune a model with poor data. So you need a huge labeled dataset to fine-tune the model correctly.

Working with longer documents. They have improved it, but still we have some problems with working with long documents.

Transparency. Deep neural network. What about interpretability? Very hard. It's a deep neural network. We will see later the number of training parameters for each model. But it's billions of parameters. So to interpret the output, it's not an easy task at all. And this is one of the challenges.

Bias. Because this model is trained on huge data from the internet, from documents. Then it comes with all the bias that is introduced by this data. So bias. And you remember the first version of ChatGPT, how it was a disaster.
