So if your data is represented by X, what are your weights? Those weights are learning something, right? So you are taking your input and transforming it to give you some output. When you save a model to your disk, what are you saving? Those weights slot into what? There are two parts to a model file: the architecture and the weights. So you can think of this as a big matrix of weights that slot into the right parts in your model. Makes sense so far.

Can anyone tell me how you train a model? When you give it data, how do you train it? What does the training loop involve? So I have my model, right? How are you optimizing the weights? How are you changing your weights? You feed in your data. Can anyone explain to me what happens with that information? How are you then propagating that back to your weights? How are you updating the weights? So the input goes through the weights plus bias, then goes through the activation function. What are you calculating there? So I have my data, I pass it through my model, it gives me some output. So say it is cats and dogs, and at the output, my model should produce some predictions. And then I have my actual ground truth labels. So then I calculate my loss using some loss function. What am I doing with that? After I calculate my loss, what do I do? What does backpropagation involve? What do you compute from the loss? The gradient, right? Can anyone tell me why you calculate the gradients? What is the whole point of this optimization problem?

Yes, the goal is to increase accuracy or decrease loss. What you are doing is, you start with a random initialization of weights. You have some data, some labels. This is a supervised learning task. So you have some data, you have some labels, and your goal is to learn this function so that given this data, you can produce the correct output. The way you do that is you feed in the data, and it computes a loss function. So let's say we use cross-entropy loss, which is your basic loss for binary classification. It doesn't matter what you use for your loss right now. But say you have a loss function, you calculate the loss, and then the goal of the optimization is to minimize it. So if this is your cost function, and this is your parameters, you start at some initial value, you are starting over here. And the goal is to reach this point, which is the global minimum that minimizes your loss, the configuration of your weights that minimizes this loss. Does that make sense?

So how do you do that? You go in the direction of maximum change, because the direction of maximum change of your weights minimizes your loss. What is the gradient? And what does the derivative give you? It is the direction of maximum change. That is why you take gradients as part of backpropagation. So what you are basically doing in backpropagation is you start with your data, you have some model, you calculate your loss, and then you calculate the gradients on the loss and propagate that all the way backwards, update your weights, then send in a new batch of data. You keep doing this until you find that you converge at that point.

And in the lab I asked some of you, what do you do with the learning rate scheduler? That is basically how big of a step you are taking in this direction. So you want to keep decreasing that step, otherwise it will overshoot and move to the other side.

Okay, so I want you to understand this before I go into PyTorch, because the whole point of PyTorch is autograd, which is automatic differentiation. So once you understand that, then everything follows, because with autograd you can basically write your own PyTorch from scratch. It is basically a way of calculating the gradients and keeping track of them. So once you understand autograd, then you basically understand most of deep learning and any deep learning library.

All right, so let's say we start with a simple function. What does autograd actually do when you give it a function like this? So the derivative of this function, does anyone understand how I got this? It is the first derivative. So when you have X squared, the derivative of X squared is 2X. Okay, so I get this.

So in PyTorch, at the back end, what exactly is happening when you give this function and it calculates the gradients? I want you to understand the concept of a computational graph. So what this is exactly doing is, when you give it a function, it creates a graph, a computational graph. The main thing in PyTorch is to create this computational graph that keeps track of all of your operations.

So if you have that function, say this is your X. So X plus 2, that is an addition operation. Can I call this intermediate value A? Then you have another operation, so this is X plus 2 times X plus 2, and then that is a multiplication. Let's say this is the output. Does this make sense to everyone? So I am basically showing that in the back end, when you give PyTorch a function like this, it will create a computational graph that looks something like this. It opens up all of your operations into addition and multiplication operations. Makes sense to everyone so far?

Okay, so I define that, and now you want to calculate the gradient. When you call backward, it calculates the gradients. So what you are doing is you calculate df/dX with respect to your X. So tell me how you would calculate df/dX.

You have two paths. So you go through the first path. Does anyone not understand this? It is important to understand what is happening here. Please ask if you do not understand.

So basically, the question you are asking is, if I made a small change in my learnable parameter, how would that change my loss? Basically what this is asking is, if I tweak X in a different way, how does that change my loss function? Because you are trying to find the configuration of your weight matrix that minimizes your loss. So you are asking, for each of your learnable parameters, how much does this contribute to my loss and in which direction should I change this so that my loss is minimized. Similarly, you are asking the same question about A and B. Does that make sense?

So what would this calculation look like? So df/dC, what is that? What is my C here? C is A times B. So df/dC, and then dC/dA. What is my C? C is A times B. So the derivative of A times B with respect to A is B. And similarly dC/dB is A.

So you see, X plus 2 times X plus 2, you apply the chain rule. What is dA/dX? Since A is X plus 2, the derivative of X plus 2 with respect to X is 1. Does that make sense? Is this clear to everyone?

Basically what I am trying to show you is how it calculates the gradients. So once you have a function, it calculates df/dC, and then it accumulates that. So df/dC, which is 3, is calculated here. Then dC/dA is calculated here, dC/dB is calculated here, dA/dX is 1 and dB/dX is 1, and those are computed here. So it saves these gradients. This is a computational graph, and each of the nodes is saving the gradients that are flowing through it to keep track of them.

So you have a function called backward. What happens in your forward pass is you take your input and you pass it through your graph and you get your output. When you call backward, it calculates all of the gradients through the chain rule and stores them.

Sometimes maybe you don't want to track gradients, and then you can set requires_grad to False. I just want to make this clear before we go through the slides, so you have a picture of what a computational graph looks like and what it means to do automatic differentiation.

So this computation graph, it is just X plus 2. You have two of those because it is X plus 2 times X plus 2. So you take X, add 2, you have two of those, then you multiply those two. So it is essentially X plus 2 squared. That is multiplied by 3, and then you get your output. Is this clear to everyone?

I just want to give you an idea of what it means to understand this concept. When you talk about PyTorch, you need to understand the concept of autograd, computational graphs, and tensors, and then you are good to go. This is not exactly how PyTorch might do it, but conceptually, this is how you could think of it. Anytime you give a function to PyTorch, this is the image you should have in your head as to how it is setting this up.

When you are doing the backward pass, you can think that for each node, you have a notion of what the gradient associated with that tensor is. If it has a parameter that requires gradients, you can set requires_grad to True or False. If you want to keep track of the gradients, you set it to True. The problem is that if you keep it True for everything, then your memory might blow up, because you can think of it as setting up arrays at every node that are saving gradients. But for validation, evaluation, and inference, you don't really need that. So you can set it to requires_grad equals False. Anytime you have certain parameters that are learnable but you don't want them to be updated, you can set requires_grad to False as well.

All right. So what is PyTorch? PyTorch is a deep learning library that was built by Facebook. The other competitor to PyTorch was TensorFlow. The advantage of PyTorch at that time was, before PyTorch, the way to do deep learning back in the 2014, 2015 era was you created the neural net and then you manually derived all of the backpropagation equations, calculating the gradients for each layer, and you implemented all of that. What PyTorch solved is you can just set up your model architecture and it will automatically calculate the gradients for you. And that is the biggest advantage you get from PyTorch.

With TensorFlow, before, it used to create this graph and then it was static. So you used to compile this into a graph object, and you could not change any of these nodes without recompiling the entire graph. Whereas with PyTorch, it creates this graph dynamically, so if you want to change a node, you can change it. And so that is why they say that PyTorch is more dynamic and easier to use. You don't have to recompile everything; you can just recompute the gradients.

So basically what I am doing here is just putting together more composable functions. And right now, when I call backward on this function, it says you cannot compute gradients. That is because the tensor does not have requires_grad set, so I need to set requires_grad to True. And so now we call backward, and it calculates the gradient. It gives 18, similar to what we calculated before.

This example is just to give you an appreciation of what autograd is doing. So when I say autograd, you should think: if you are given a function, when you call backward on that function, it will automatically compute the gradients and keep track of them. Makes sense?

So 18 is because I am calculating the derivative at X equals 1. So it is this function evaluated at X equals 1. I am defining X as 1 here.

You can also access the gradient. You can get the gradient value. That is one way of debugging your neural nets, to see the gradients over the different iterations. And that is the whole point of this computational graph: you can basically go back through the gradients. There are different training techniques that actually leverage these gradients. And I think one of you had gradient clipping in one of your hyperparameters. The problem when you are training really deep neural nets is the gradients can blow up or they can vanish. And when they blow up, you want to clip them. So that is another way of regularizing the networks. That is the whole main advantage of PyTorch: you can think of each of these nodes as having storage where it is storing the gradient at each iteration. So you can actually use that for debugging and do other kinds of interpretability things and so forth.

Does everyone understand the concept of tensor, computational graph, and autograd? So basically, the first derivative of this function: whenever you have X squared, the first derivative of X squared is 2X. So the first derivative of this is 2 times this expression. Does that make sense?

So what are the basic building blocks of a PyTorch pipeline? Can someone explain to me what the basic building blocks are? If you are writing in PyTorch, what are the main things you need to have as part of your pipeline? You have a function that does your data loading. Then what else do you need? Model definition. Training loop. Then what? Evaluation. Then you need your main function to tie it all together. Let's go through each one to see how you do it in PyTorch.

In PyTorch, you have a notion of a Dataset and a DataLoader. You can define your data using the Dataset class. So if you have your own data, there are multiple existing datasets that are preloaded into PyTorch, like MNIST, CIFAR, and so forth. But then you have your own cat and dog images, right? You have your cat and dog images or your flower dataset on your disk, and you set it up in such a way, similar to how you set it up for your MLP, where you have, say, your flowers dataset, and then you have daffodil, and then you have image one, image two, then you have your tulip, and so on. It is a folder structure.

So PyTorch allows you to ingest your own dataset, and the way you can do that is by creating a custom dataset which inherits from the Dataset class. To define your custom dataset, you need to define three things. First, initialization, which will basically define the size of the dataset, what is the path to the data, how to assign the labels. So you would say that your data is at this path to the flowers directory, and then you would say to take the folder name as the label for these images. So in initialization you define what the labels are and where the data path is, basically telling it these are the files and these are the labels for these files. It can also just be a CSV that you are reading from where the paths are, and you read that and assign labels for each of the data points. Make sense?

The second thing you need to define is the length of your dataset. So when you call len on the dataset, it will give you the size of all of the training examples in the dataset. So this will just be the length of whatever your dataset is.

The third is get_item, because the DataLoader is a generator. When you call next, get_item defines how you want to retrieve and display the image or your particular training example. So if your dataset is an image, maybe you want to show the image, so then you put in code here that will just show that image. Maybe your dataset is an image with some sort of facial landmarks, and you want to show all of those. All of that code will go here. But these are the main three things that you need to define to create your custom dataset.

For all of you working on lab 5, I know a lot of you have used ImageFolder. I would suggest trying out writing your own custom dataset for the same thing and see how you can actually implement this. Once you have implemented your custom dataset, the way you call it is by just calling DataLoader and then you create an instance of your dataset and then you pass it to your DataLoader. Does that make sense?

Let's go through an example. Say I wanted to create a dataset with two classes. In my __init__, I will provide the root directory, if there are any transformations that I want to do specific to that dataset. So maybe I want to crop the images or whatever different data transformations you want to do, you set that up here as a chained pipeline. And then you assign labels to the different classes. So you are just saying, for these classes in this directory, assign these class labels. It is just basically telling you, if you read from this directory, all of these images are this class name, all of these images are that class name. It is just reading the data and assigning labels.

In the assignment, you use ImageFolder, which basically at the back end is implementing this. So I am saying that if you use ImageFolder, which is a shortcut, you can basically write your own custom dataset for the same thing. Makes sense? When you are loading in your __init__, what do you want? You want to know where the data is stored and which of those data samples has what labels, right? That is what you do in your __init__ function.

Then the __len__ will just be the size of your dataset. And the get_item is basically, every time I call next on my dataset, it should show me an image. So I want to open the image and also apply whatever transformation. So say it is a grayscale image and I want to show it as a color image. Maybe my transformation is converting it from grayscale to RGB. So then it will apply the transform.

So PyTorch allows you to set up your own custom dataset. Does this make sense to everyone, how to define your own dataset and how to load your own data? For all of you working on lab 5, I highly suggest trying to write your own custom dataset, just so that you know where to give the paths, how to give the labels, and how to read in the data.

Once you have defined your dataset, the way you put it in your DataLoader is you call any transforms that are universal. Your DataLoader would read your training dataset, validation dataset, and maybe your test dataset separately. And maybe you have some transforms that apply to all of those, and those can go here.

Then you basically create an instance of your dataset class and pass it through your DataLoader. The DataLoader is a generator that just wraps your dataset class. So you can basically call next on the DataLoader to get the next batch, or the next batch, or the next one. Makes sense?

So here I have a training DataLoader, a validation DataLoader, and a test DataLoader. The dataset that goes in here would be the training dataset for the training loader, the validation dataset for the validation loader, and so on. I will go through a full example, but this is just to give you the pieces of how you define the dataset and how you call it through the DataLoader. Makes sense?

Once you define your data, you can also define a custom layer. PyTorch gives you a whole bunch of different layers, like convolutional, linear, and so on. But say I wanted to create my own small module that I want to reuse each time. You can do that through a custom layer, and all you need to define are the __init__ method and the forward method.

Say I wanted to create a custom linear layer. The linear layer is basically implementing the function AX plus B. If I want to implement this, all I need to do is, in my __init__, I initialize my parameters, and then in my forward, I define this function. So I define the total number of input features and output features, defining the size of that layer. These are parameters that I take in my initialization for input and output. Then all I am doing is defining the weight and the bias and initializing those to random values. Those are now my two learnable parameters for my custom linear class.

What we are doing is, I am just giving you an example of how, if you wanted to define your own layer in PyTorch, you could do it. PyTorch gives you a fully connected layer, a convolutional layer, and so on. But maybe I want to create a custom layer, and that custom layer has maybe one type of operation that I keep wanting to reuse and I do not want to keep rewriting. So I can create my own custom layer. Say it is a very arbitrary function. So I want to create a new layer that is always going to compute this function. Then I will create that where in __init__ I define my learnable parameters, initialize those, and then in the forward I implement that function. So it is just telling you how to take your input and transform it. Here basically what it is doing is WX plus B. But this can be anything. I want you to understand that you can basically implement anything.

So basically, what PyTorch is, is it allows you to do autograd, and everything around it is bells and whistles. You can write your own custom layer, your own custom DataLoader, your own custom loss, your own custom optimizer. I am just showing you how you can do it and what the basic things you need to define are. For your assignment, you will not have to do any of this, but I am trying to get you to understand what you can do with PyTorch. Makes sense?

So to define your own layer, all you need to define is the initialization method where you initialize any of the learnable parameters, and like the input and output size, along with any other parameters as part of your custom layer. You initialize that and then define the forward method that implements the function. You do not have to define the backward because the backward is done for you automatically. Unless the differentiation is not defined for some operation, then you might have to tell it how to calculate the backward pass. There are some cases where there are functions that are not fully differentiable, and then you have to do approximations for those differentiations. In that case, you might have to provide a backward method and tell it, for this function, this is how you do the backward pass.

And then, again, for the loss, you can define your own loss. They are all inherited from this thing called nn.Module, which is the main thing that you use for losses, layers, and everything in PyTorch. And again, for the loss, it is just __init__ and forward. You do the same thing where, in the initialization, if I wanted to implement a simple mean squared error loss, what is the mean squared error loss? It is basically the average over all my data samples of X predicted minus X actual, squared. So the mean of the squared loss for all my samples. That is basically what is implemented here: just the mean of the prediction minus target, squared.

So this is just to give you an understanding that you can define your own dataset, you can define your own layer, you can define your own loss if you want. For the dataset, you need three things: an __init__ method, a __len__ method that gives you the size of the dataset, and a __getitem__ method that tells you how to retrieve something from the dataset. For your custom layer, all you need to define is the initialization and the forward pass, the function you implement. Similarly for your loss, it is the same thing: your initialization and the forward pass.

So before we move on, can anyone explain to me what you understand about autograd? And what parameter would I change if I did not want to track the gradients on a particular tensor? requires_grad. Okay.

So we mentioned that as part of your data pipeline, you have data preparation, model definition, training, and evaluation. So how do you do the data prep? In your assignment, you are asked to create a load dataset method that will take a dataset. The custom dataset approach we discussed, you can use ImageFolder, which is a shortcut that basically implements getting the data paths and loading it, except that the ImageFolder function requires your data to be in the format where each of your class labels has a folder with all of the images under it. So it automatically takes the folder name as the label. Does that make sense to everyone? Can you explain to me what ImageFolder does? It creates the label mapping that is based on the folder structure of the data. Okay.

So like I said, you can apply different transforms. Why do you need to resize your images? What happens if you don't resize? The main reason to resize is because maybe in your dataset, each of the images is a different size, and if each of the images is a different size, it will give you an error because it is trying to batch them, and for it to batch, to be able to stack, they all have to be the same size. So in this case, you are just doing cropping and resizing, but say if you had text, some of your transformations would include tokenization, padding, and so forth. So under your transforms, all of the preprocessing for the data goes in.

Then you read your dataset. If you had implemented your custom dataset, this would be your custom dataset class. Then you split your data into training, validation, and test, and then you load it with your DataLoaders. You basically split your dataset however you want with whatever split sizes you have, and then you have separate DataLoaders for each of your splits. You can think of the DataLoader as just a generator, so if you call next on it, it will give you the next batch.

Where did you get the mean and standard deviation numbers? Those are from ImageNet. It is kind of standard practice to use them in computer vision, especially for classification tasks like cats and dogs. Do we have to always enter those manually? So generally, if you are using any other dataset, what you would do is calculate the mean and standard deviation from that dataset, from just your training split. But in computer vision, they often just use the ImageNet values because you also want to generalize well, and ImageNet was a huge dataset back in 2017. These days it is considered a smaller dataset, but this practice has just carried on. But you could also normalize with the mean and standard deviation of your training set. Never use your whole dataset because then you are cheating, because that is data leakage.

So you have defined your data, you have loaded your data, you have created your DataLoaders. Next is to define your model. To define your model, say this was your model. Model definition in PyTorch is kind of like Lego. These functions have been defined for you: convolution is defined, ReLU is defined, pooling is defined. If any of these were not defined, then you would create your own custom layer for that function. But right now in PyTorch, all of these different things have been defined for you. So now you look at this architecture. When you are creating your own architecture, you first draw out the architecture that you want, and then you implement it just like Lego blocks when you are writing your model definition.

So the way that looks is, that same architecture is implemented here. You first define all of the different layers that you need as part of your model. As part of your model, you have convolutional layer one, convolutional layer two, your flatten layer, your fully connected layer one, and your fully connected output layer. You basically define all of that in terms of the dimensions, the kernel sizes, and so on. And then you define how they are used.

In the initialization of your model definition, you are defining all of the layers that you would use. So if you had a fully connected layer that had 64 inputs and 32 outputs, that would be FC1. Then if you have a second fully connected layer with different dimensions, that would be FC2. So here, you can think of the initialization as initializing all of your layers. You are basically going and getting your Lego blocks and setting them up.

Then in your forward pass, you are putting together that function. In your head, you should always think of a neural network as a function. In your forward pass, you are putting together that function with the different layers. Does that make sense?

In the forward pass, I am doing a convolutional layer and then I apply ReLU and max pooling. Similarly, again, ReLU and pooling applied to the second convolutional layer, then I flatten it. Can anyone tell me what the negative one does here? What does flattening mean? So, yes. Say my convolutional layer is using 32 filters. What is my output going to be? How many channels is my output going to have? 32, right. So when I flatten, I want to flatten it all out.

What is negative one? Negative one tells it, because your input is going in as batches, when you say flatten, you still want it to be batch by everything. So say my output from my convolutional block is batch by number of filters by height by width. So it is like, my batch size is 64 and then 32 by 32 by 1, or whatever. When I want to flatten it, I still want 64 by the product of the rest. But I don't know what my batch size is. The negative one is telling it to infer the batch size and set that up, then multiply all of the other dimensions together. Does that make sense?

So negative one is telling PyTorch to infer the batch size. Your first dimension is almost always your batch size. So you want to flatten something like this with 64 as the batch, and you want to flatten the rest of it. But you don't know the batch size, so negative one tells it to infer the batch size.

That is how you define a model. Any questions about how to define your model or your dataset? You said these layers are already built in, right? So all these Lego blocks are built in. So why are we defining the forward? Because this is your arbitrary model. Maybe I want to create a new model that has three convolutional layers, two feed forward layers, one max pooling, one mean pooling, and so on. These Lego blocks are predefined, but your custom architecture is not predefined because that design space is infinite. So you can think of this as red block, blue block, green block. I want to create a building with two red blocks, one blue block, one green block, one yellow block. That is your forward function that you define, but that is custom to you. Does that make sense? And again, these could be your own layers. I can create a custom layer if I want, but I just have to define it like how I defined the custom layer earlier. And when you do stuff with Transformers and these other models that have modules within them, what they do is they create those modules as separate blocks so that it is easy for you to build.

The numbers like 64, 56, and so on, those are the layer dimensions, the kernel sizes, not fixed values. And same for the ones about 128. The whole point of this is you create your own Lego blocks. So you are saying you want a layer that is 4 by 6 here, and maybe you want to use that multiple times. So if you are building a network, you might use 4 by 6 blocks in one place, then 3 by 3 blocks in another. So then you have to define each of those separately as two different layer instances. But it is still the same type of layer. So it is still a linear layer, just with different sizes. Makes sense to everyone?

So I have defined my DataLoader, I have defined the model. What is left? The training loop. So given the model, what is the process? I already split my data as part of my data preparation. So now the DataLoader is a generator, so you iterate through your DataLoader, which gives you a batch each time. Each batch is given to your model, and you update your parameters. What does that mean? You give the data in through a forward pass, then you call a backward pass, calculate all of the gradients, accumulate all of the gradients, and pass it back to all of your learnable parameters. That is basically what training a neural network involves.

The two most important things to define are your loss and your optimizer. The hyperparameters are associated with the loss and optimizer. Learning rate is a hyperparameter for your optimizer, along with momentum and so on.

So why do I need to zero the gradients? If you go back to our computational graph, when it calculates the gradients, it calculates the gradient here, the gradient here, the gradient here. But it does not automatically flush out these buffers. So you have to manually flush out these buffers, otherwise you keep adding to the previously calculated gradients. The reason it does not flush them out automatically is because when you are doing sequence labelling tasks, you want to backpropagate the gradients over time. And in some cases, you need to keep accumulating them. So that is why here you need to manually zero out the gradients, otherwise it will keep accumulating those older gradients and add them to your current gradients. Make sense?

So your model is implementing the function, and you do a forward pass. There is this convention in PyTorch where if you call model(inputs), it is the same as calling model.forward(inputs). You take your inputs and apply the transformation that is defined in the forward method. Then you define your loss, then you do a backward step, calculate all of the gradients, accumulate the loss, and keep doing this for all of the batches until you converge.

How do we know that it converges? What you are doing is, every time during training, ideally you want to keep plotting your training loss over iterations. And maybe every 5 or 10 epochs, you also want to plot your validation loss. Then you will see that your training loss is going down, and your validation loss should also be going down. But sometimes at some point your validation loss might start increasing, so you should cut it off there. Because otherwise, you overfit.

It is always good practice to keep plotting your training loss as you are training, because if you see your training loss in the initial iterations going up, there is something wrong in your data loading, in the way you are setting up your learning rate, there is something wrong in your code. And so then you go and find the bug to fix that, because at least initially it should decrease.

Another hack when you are training neural networks is, when you have written your code, start with a really small dataset and try to overfit to that dataset. If your model does not overfit to that, that means there is a bug in your code. Because these neural networks are function approximators. Whatever input and output you give it, it should learn that function. And so if your dataset is really small, it should memorize that data. If it does not, then there is something wrong in your code.

So once you have done your training, then you do your evaluation. For evaluation, you notice that I have written torch.no_grad() over here. That is basically saying, for this entire block, putting this here means I am setting requires_grad to False for all of these tensors. For any of the learnable parameters, anything under this block, all of these are not tracking the gradients, because this is evaluation. I do not need to track the gradients for these operations.

Why do we need no_grad? Because otherwise, initially you had set requires_grad to True because it was part of the training. Before, you had set requires_grad to True, so those gradients were accumulating. Now when you are again passing through those same learnable parameters, if you do not set this up, it will still track those gradients. And you do not really need it. It will not affect anything, but it is just best practice to put it because sometimes you do things there that might cause it to calculate gradients. If you do any non-trivial operation, it might calculate the gradients for those. So it is just best practice to include it.

So that evaluation was on the validation set, and I have the same thing for the test set, which is a totally separate held out set that you want to get the predictions for and calculate your confusion matrix and so forth. And that is kind of the entire pipeline. Basically you have loading the data, defining your model, training the model, evaluating, and then the main function that ties it all together. Does that make sense?

You had a validation set as well, right? Yes. That is because the code I was using previously did not have a validation set. I was trying to change it last minute. But after you have a validation set, the way you use it is in your training loop. When you are doing the training, you say that every N epochs, every 5 or 10 epochs, you do a pass through your validation set to get your predictions and the loss so you can plot it on your learning curve.

And then you add the validation back to the training and retrain? It depends how you set up your training. If you have very little data, you could do K-fold validation or leave-one-out validation. But if I have at least, say, 24 training examples, then I will take 23 and keep one as a validation set, validate on that, then shuffle it, put that back into my training set, and keep another one out. But these days, datasets are pretty big, so I would rather have a separate training split, validation split, and a test split. But K-fold validation is also valid, where you separate your set into K folds, then you take K minus 1 as your training set and the last one as validation. It is basically leave-one-out but on groups of samples. But the easiest approach, especially with slightly bigger data, since you have enough to get statistically significant results, is to just have separate splits.

So that is the main thing that you do in PyTorch. Then there are other things you can do with PyTorch where you can train on multiple GPU devices. You can say that you want to load these tensors to this device and load those tensors to that device. Especially if you have really large datasets, there is a notion of data sharding and model sharding because your GPU has limited memory. The VRAM of your GPU is finite. If you have a model like LLaMA that is like 400 gigabytes or something, that is not going to fit on one GPU. So what you do is, you can think of the model as one big matrix. You take this part of the matrix, put it on GPU one, take this part of the matrix, put it on GPU two, and so on. Similarly with your data, if you have a huge dataset, you take this part of the dataset and put it on GPU one, this part on GPU two, and so on. The whole point is how do you accumulate the gradients, because each of these GPUs are running their own part. So you basically have to synchronize the training. One finishes, then you take the gradients from there and update everything else. Those are more advanced training techniques and how to make your training more efficient.

You should be aware that there are things called model sharding and data sharding. Once you get comfortable with using PyTorch, I would suggest looking into this so that you know how to leverage multiple GPU devices. What to give to your CPU, what to give to your GPU. Because the GPU is good at repeatable actions. You give it one function and it will repeat that on all of your data in parallel. Whereas the CPU is good at sequential operations. So you should know what operations should run on your CPU, what operations should run on your GPU, and how to do that.

A lot of the work these days is basically how do you make all of this more efficient. Because you can basically write autograd code in about 100 lines. I would highly suggest looking at Andrej Karpathy's series called micrograd, where he implements PyTorch autograd from the bottom up. You get a good sense of what exactly is happening when you do backpropagation. I would highly suggest looking at that series, especially if you want to understand the internals, because it really helps. None of this is strictly necessary to know. Without knowing all of this, you can still implement neural networks in PyTorch. The problem comes when you are debugging. To really understand what is happening in the backward pass, it helps to know how these gradients are calculated and where you might be getting exploding or vanishing gradients.

So tensors are just a data type. The only point is that tensors allow you to keep track of gradients, so you use tensors instead of regular arrays. Autograd, I hope everyone understands what autograd is, or at least you have a sense of it. It means that it is keeping track of the gradients at every computation. The nn.Module is the main building block in PyTorch. In the training loop is where you define your optimizer and loss function.

One thing I would suggest to everyone for the assignment is, especially if you really want to understand how to use PyTorch, because PyTorch is, again, these days unless you are actually training neural networks from scratch, you will not necessarily need PyTorch. A lot of companies really require you to know LangGraph and agent stuff. But it is good to know PyTorch, and if you really want to learn it, for your lab 5 I would suggest implementing cross-entropy as a custom loss, implementing one of the modules as a custom layer. You already have the ground truth for that because it is implemented in PyTorch and all of this code is open source. So you can go and see how Conv2d is implemented, and you yourself can implement your own custom convolution and compare where you went wrong and understand how it works. So I would suggest for your assignment and just for your own learning, implement a custom dataset, implement a custom layer, implement a custom loss, and then you will basically know how to use PyTorch.

Once you understand the concept of autograd, that basically is PyTorch. In about 100 lines of code, you can write PyTorch. Everything around it is all about increasing the efficiency of how you move tensors from GPU to CPU, and so on.

You also mentioned purposefully overfitting the model. Yes, so this is just a deep learning debugging process. Once you have written your code, instead of training on your entire dataset, just take a very small subset of your dataset. Take like 10 examples, and it has to overfit to that. So if your training curve does not go down very fast, then there is something wrong in your code. Overfit to that small dataset so that every time you give it an example, it has to predict the correct class most of the time. If it is not doing that, then there is something wrong in your code. That is a very easy way of debugging because it is very easy to write wrong code and still get seemingly correct results.

There are also different ways of looking at the gradients to make sure things are working, especially when you are initializing parameters. Do not initialize everything to zeros because that will really hurt your training. Initialization is very important in neural networks because backpropagation is very sensitive. The gradients can blow up or they can vanish very easily, so you need various tricks to handle that.

This will be like your bread and butter. If you do any kind of ML engineering, this is what you do.

In tomorrow's class, I will be covering transfer learning. The syllabus originally had MMPretrain, but MMPretrain was a good library about three years ago. The professor who created it passed away and nobody is maintaining that library anymore, and it has become outdated. The Transformers ecosystem has moved on and it has not been updated. But the engineering behind it is still good. I think you should also learn Hugging Face. There is no Hugging Face in your syllabus. I just added it because I think you should know it. So in tomorrow's class, what I want to do is show you how to do transfer learning through Hugging Face, through PyTorch, PyTorch Lightning, and through other tools. And then you can decide what you want to use.

If you want to work as an engineer, the things you need to know are Python, PyTorch, Hugging Face, LangGraph, and some library that does observability, like LangSmith or Arize or something like that. And yeah, there are a lot of job openings. If you look at job postings, there are a lot of openings for what they call AI engineer. But the way they describe AI engineer varies. Some of them basically want a software engineer that can do API calling. Some of them want data scientists. Nobody really knows what they want these days. But if you just know LangGraph and the agent stuff, you are in good shape.

A lot of the agent stuff is more engineering than ML, because it is basically how do you orchestrate different things. It is knowing how to do distributed programming and those kinds of things. These days, you are not really training models from scratch that much. And even if you are training, people are doing this thing called LoRA, which is where you freeze most of the model and you are just training some small part, because the models are so big. So actual model development is relegated to the really big AI labs. What you do as a startup or even as a smaller or medium business is mostly API calls. But knowing how to debug stuff, knowing things like someone is using validation data in the training set, those are things you should know. A lot of times people will ask why you would use this model versus that model. So you should know the advantages of different models and be able to talk about them.

A lot of times they also conflate data engineer with AI engineer. A data engineer is setting up data pipelines and all that. For that, you need to know how to do MapReduce pipelines and those things.

A lot of the schools are facing the same problem, which is that they have their established syllabus, but what is required in industry these days has changed. Also, I would highly suggest learning how to use code generation tools. Learn how to become efficient with Cursor and all of these tools, because these days a lot of the code is going to be generated for you. You should know how to guide it, like how to set up the guardrails so that it is only going to modify that function and not your entire codebase. I think Stanford has a modern software development course with modules on how to use a lot of these tools. I would highly suggest that.
