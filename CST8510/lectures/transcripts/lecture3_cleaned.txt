But with deep learning, they have come to grow, creating their features in general as representations. For example, if you take a deep learning model, you take different layers of convolution and pooling, things like that. And all of these features, right, very raw, you know. But then if you go further downstream in the model, then what happens is the model itself is creating internal representations. You have these features like the edges, corners, over the layers, which features start up here, and through the various internal transformations, not transformations as in the transformer, the model itself creates generalizations, higher order representations, like features. Hence, these days, particularly for image data, you go with the data, and as part of the modern training it internally develops these features. That's one of the big advantages of deep learning. This is for audio as well, and to some extent, text, right?

In text, earlier people used to create features like bigrams, trigrams, right? And people used to use synonyms to replace, you know, some of the things, things like that. So people used to do feature engineering even in text. But these days with transformer models and representations like embeddings, right? So it was that numerical representation, right? Once you have the numerical representation in these vectors, right, that is the representation. For text data, these days, nobody much creates features because pretty much once you tokenize it and use any kind of embedding type of representation, like if you just use the embedding, that will work. For example, if you want to do sentiment analysis, right, you find you have some model like a sentiment analysis model with some examples and just use the embeddings, that will do the job.

But then why do you need feature engineering? Today I'm going to talk about why you want to understand that. You need to understand that in industry they don't always use deep learning because of various reasons. Many industries require you to explain the results. For example, it is mandatory that in insurance or financial institutions, when somebody applies for a home mortgage loan or credit score, you need to explain why that score or mortgage was rejected. So if you just use a deep learning model, you may not have enough explainability. Similarly, if you are in cybersecurity, right, if you need to explain to the analyst or the stakeholder why this is an attack, this particular user seems to be suspicious, right? And so a lot of various industries, including research, don't use deep learning models in various domains.

And also if you look at typical data like log data, where you get logs, a lot of logs, this kind of automatic feature creation does not work because it's from the software. You have a lot of fields, nested fields, you need to understand the schema and then, you know, make some transformations, and you want to do some aggregations because logs are produced continuously, right? There will be a lot of them. So in various domains like that, you know, if you deal with log data, if you deal with a lot of data related to finance, healthcare, you really don't use the deep learning models, or for the type of data you deal with, it still requires feature engineering. Okay, so it is still important. So just because the embedding models are there doesn't mean that they are used everywhere to avoid feature engineering.

So, just as an example of what people used to do earlier with text, the original text "I have a dog, please," right? So you then use some libraries like NLTK, Natural Language Toolkit. So you take and remove some of the stop words. The stop words are like "a" which appears in almost every text, right? So they are not informative. They don't carry any information. If you remove that, you get words like "I love dog is sleeping." Now, there is a problem because these words, in another text, might say "I have a dog, he left just now," right? So sleeping, slept, sleep, all these are the same thing. So you do this kind of lemmatization, you remove these characters, then keep the root of the word. So you convert this, the simple "I have a dog," the contraction "I have a dog." Then you make it lowercase. Then you do lemmatization. So after this, you can convert this to the base form. So the simple "I have dog, I have my dog." So "I have, I have dog, dog." So this is just features.

Yeah, so when we talked about this yesterday, one thing I was wondering about. You have a different example, "I have a dog, I have a dog, he is sweet." So the pronoun "he," you don't know necessarily if he's referring to the dog or someone else. And you need maybe more context. Yeah, so that is important. But the key thing is that these days you don't do this kind of processing for text as much.

What you still do is tokenization. Tokenization is the standard first step. Then what you do is convert the words into numerical representations. You just use embeddings because you need to have numerical representations.

So the earlier approach was that text used to be processed using NLTK. But today, people do tokenization, which is the standard first step, and then use a numerical representation like embeddings. But of course, you know, there are things like there's a library called FastText from Meta, right? So there's a library for text classification. It still uses this approach. If you want to do very fast classification, right, use that. That is based on text and is fast for speed. So I was just showing you an example of how you would create features in a very traditional way.

So now we are talking about something else, which is before you start building your model, you might have to handle missing values. Because this is, again, very, very important. Missing values arise particularly in various scenarios, right? For example, if you are looking at analyzing surveys, right, it's possible that people don't fill in all the fields, right? Because they don't want to give that information or for various reasons. So you have a lot of missing values. Then you think of logs, logs from IoT devices, logs from your endpoint agents. So what happens is that these logs are collected locally, and once in a while when the buffer is full, they push it to the cloud. That's how they work. Now, it's possible that sometimes they sample the logs. They don't send all the logs, or at the time of sending the logs, especially over the network, the data does not transfer fully. Okay, so you know, a lot of missing rows or missing values, all that can happen. So missing value treatment is a very important aspect, particularly when dealing with customer data.

So there are three types of missing values. There is missing completely at random, missing at random, and missing not at random. Okay. So this has to do with whether these missing values are dependent on other variables or other rows and things like that.

So what missing completely at random means is that the probability of a value being missing is not related to, you know, whether your gender or your income or anything, right. So for example, it's possible that for a particular gender, they probably don't want to disclose their age so much, right. So what is going to happen is that if you look at that gender, they may not disclose their age. So that case is not missing completely at random. In that case, the probability of a value being missing is related to other observed variables in the data set. Okay, but not to the value of the missing data itself.

Or, for example, if I have a certain illness, right, then I may not want to disclose some of my medical status, right? Or something like, they ask me in a healthcare survey, what's my blood pressure or some kind of measure? If I have some disease, I'm not going to disclose, right?

So as an example, for age, where there may be in some cases it's random within the gender, whether they disclose their age. Like, maybe some people who are a certain age don't want to disclose and that is random. But other times they're not random. Yeah, so it's dependent on gender but also on other things. So what I'm saying is that there is some correlation between a missing data point and an observed variable. That correlation does not need to be perfect, it might be partial. In all those cases, you classify it as missing at random.

So then there is the third class, which is missing not at random. Okay. So in that case, basically, the probability of a value being missing is related to the missing value itself. Okay. I'll give you an example for that. For example, income, right? Then it's like high income people may not want to disclose their income. Or low income people may not want to disclose. So it depends upon that missing value itself, not related to any other variable. So the dependency is on the value of that variable itself. So that is called missing not at random.

When it is missing and it is not related to anything, whether not related to itself or not related to any other variable, it is called missing completely at random. Okay, so a little confusing terminology, but you need to keep this in mind.

So, given some examples here. Missing completely at random: in a survey, respondents accidentally skip a question about their favourite colour due to a printing error on some forms. Okay. So the printing error has nothing to do with that person's age or gender or anything. So it's completely at random. That is an example of that.

Now, missing at random: in a medical study, younger participants are more likely to not report their income. So the missingness of income is related to some other variable, age, not to the income value itself. So younger people, they don't report their income whether it's low or high, regardless.

Missing not at random: in a survey, people with severe depression are less likely to answer questions about their mental status. Makes sense, right? So that is because the value of the variable that is missing is related to the probability of it being missing. It's the value of the field itself.

So it's very easy to remember. If a missing value does not depend on anything, it is missing completely at random. If the missingness depends on another variable, it's missing at random. If it depends on the value of that variable itself, it is missing not at random. So that's about missing values.

Now, how do you handle missing values, right? So you can do things like column deletion, row deletion, you can delete the entire column. For example, you know, you could delete the column for age and mental status, right? Or you could delete some rows, like row one, row three, row six, et cetera. That's one option. But this you should do carefully. Usually, there will be more rows compared to columns in any data, right, any good data. So if you have a few missing rows, then it's okay, otherwise, you know, removing rows should almost never be done. Okay. So that's not a good suggestion generally.

Then you can impute, right? You can replace the data. If it's numerical data, right, you can replace it with the mean or median, or if it's ordered data, you can replace it with some kind of interpolation, right. But that can lead to a lot of problems, because one problem is that it can create some bias in the data, because the interpolation depends upon what the percentage of other data is, right? If there's not enough other data, right? So for example, let's say in this case, here you see the income column is $50,000, right? There's only one income which is $10,000. Maybe if you replace rows four and five with the mean or mode or do interpolation, right, it may create bias, because those values are high, right? Maybe rows four and five income are maybe $5,000. So you can create some kind of bias in the data.

But this is often used in practice, which is better than just deleting the data. It can also cause another problem, because when you do anything like this interpolation, you are creating some dependency, right, between the data. Let's say you are doing imputation for income using some mean or something, right? Then that mean is dependent upon other values. Now, let's say you then use this data for creating a model. You split the data randomly. And it also happened that these imputed rows came in the test data, right? So now what is happening? In the test data, right, you want to produce some labels. The label would be whether this person is, you know, let's say rich or poor, or high or low, whatever, right? Suppose you want to use this for credit scoring or classification, right? This wasn't the original data. But now you're creating a dependency between the training and test data, right? Ideally, the test data should not be seen by the model, right? The test data should be completely independent. But during this imputation, some information from training is leaking into the test data. That can cause the model to overfit.

What you should do is first do the splitting, then do the imputation. So you split first, and within each split you do the imputation separately. That's more accurate. That's correct. So be careful about things like that.

Okay, another thing you need to do is feature scaling. So feature scaling is important because, see, for example, right, the age, the age ranges from, say, zero to maybe 100 or 200. Whereas income can go from, you know, a few thousands to hundreds of thousands, right? So that range is very huge, right? Whereas the number of children is always going to be single digits, right? Now, when you pass this to models, you know, let's say a regression model or a classification model, the model doesn't know or understand that, you know, age is something that goes between certain ranges. It treats everything as a number, right? So when it estimates the model parameters, right, it can cause more errors. The errors associated with the estimation of parameters can cause issues. So it's ideal to make all the variables in the same range. Okay, all numerical features in the same range.

So this is done by various transformations. A simple one is min max scaling, which converts your variable into a zero to one range. So this can be done using some simple scaling, right? So subtract the minimum value from each value, then divide by the max minus the min. So this transformation will convert your features to the zero to one range. And of course, for categorical features, you do encoding, right, one hot encoding and other types.

You can also do, you know, another transformation to convert them into some kind of a Gaussian distribution using what is called a Box Cox transformation. Because usually we assume that the variables satisfy some kind of normal distribution, the noise satisfies a normal distribution, particularly in, you know, problems like regression. If you see a distribution that is skewed, right, then it's better to transform it to be closer to a normal distribution. So this is another preprocessing step you can do.

So another thing is to convert some of the numerical features into categorical features. Okay. By converting them into buckets. For example, age, right? Instead of using age directly, you create buckets for age ranges, like zero to ten, and so on. It depends on your problem. You might have five different bucket sizes. Because what happens is that when you take age, right, you see age is actually an integer variable, right? And you see in the population, you don't see a nice smooth distribution for age. You don't see a lot of values for each individual age, even if there's no missing data, right? So it's better to aggregate. So for example, you may see one or two cases for children age one, two, three, five, like that. You may see a lot of ages like 30, 31, 40, 50, like that, right? Again, you don't see as many ages above 60, 70 like that. But if you convert into buckets, right, that sort of reduces the noise and makes that distribution more smooth. Okay. So it's better, you know, for quantities like age, to convert them into ranges and into discrete quantities by doing this bucketing rather than using continuous values for modeling. So I mentioned using reduced cardinality.

Now, for high cardinality features, right, for example, there are things like zip codes or IP addresses, right? IP addresses are like almost infinitely many. So many IP addresses are possible. And if you keep them like that, they are not useful. You can, you know, convert them into some kind of embeddings. So that is another approach.

So this is one way to do that, using embeddings. So embeddings have the advantage that they will preserve semantics, particularly for text. You can figure out a way to convert these into numerical representations. So basically, what embedding means is you learn a numerical vector of a fixed size. So your words can be, you know, your sentence can be quite long, right? And a sentence is composed of words. Okay. So for each word, you create a numerical vector representation.

So here, for example, right, there's a word "cat," right? So let's say you create an embedding of size, you know, one, two, three, four, five, six, seven. Okay. Now, you take a word. The embedding will give numerical representations so that, for example, "cat" will be very close to "kitten." So you see the vectors, right, are very similar, very close. But if you take something like "house," right, that will be much different. So if you think of this as a numerical vector, right, in the space of that numerical vector, the words which have similar meanings, they will be closer. Okay. So the algorithm which creates these embeddings is constructed such that the values of the numerical vectors for words which are similar in meaning will be closer, mathematically similar.

Similarly, you see "man" and "woman," right? And "king" and "queen." So for example, right, if you take the difference between "man" and "king," you get the same difference as between "woman" and "queen." Okay, so this is because of that structured nature of embeddings.

So there are various algorithms for creating word embeddings. So this idea of what is a word embedding: a word will be converted into a numerical vector of fixed size. You can choose that size, whether it's 500 dimensions or 10 dimensions. Typically there are a few hundred dimensions. Then you take a sentence, and you can convert that sentence into one vector by taking the embedding of each of the words in that sentence, then doing some kind of pooling. You do a max pooling or an average pooling and things like that. Basically, average across all the words and then create one vector for the sentence. You can also concatenate them, but then it becomes a very large vector.

So embeddings are today the most used representation of words as features. As I mentioned, the reason is they preserve semantic relationships. Okay. So the embeddings of words which have similar meanings will be closer. Okay. So if your model is trained with "dog" in the vocabulary, when at inference time there's the word "kitten," it understands that it is talking about something similar to "cat." And if something is about "house," it's very different. So there's a nice article if you want to learn more about this.

Now, how do you create these embeddings? There are different models. So the first model which came about 10 years ago, which created the first embedding model, was Word2Vec. So that model, what it does, it had a small neural network. So that neural network, you could create this task where you have a sentence, and you have the current word, the previous word, and the word previous to that, the next word, and the word next to that, right? So these are the words around the target word. So you predict this word. So given the surrounding words, predict this word. So this is one formulation: given the surrounding words, predict the center word. Or the other way: given a word, predict the surrounding words. So they built that model for this purpose. There are two different models, and both ways you can train it.

So when you train this model, that neural network, the middle layer, right? So there's a neural network with the input layer, hidden layer, and output layer. That hidden layer produces the embedding. Okay? So that's how you create the Word2Vec embedding.

Then there's an improvement of that from Stanford called GloVe. So GloVe was developed by a group at Stanford. But then, nowadays, you use models particularly like BERT, the first transformer model developed by Google, right? Bidirectional Encoder Representations from Transformers. So that can be used for creating embeddings. You train that on the data and choose some layers, typically not the last layer. The last layer will typically be, you know, a logistic kind of layer which converts into some kind of probabilities for various classes. So don't take the final layer, but take one layer before the final layer. That layer will be a numerical layer. The last layer will be the layer that converts into some kind of probabilities for class types. So don't take the last layer. If you take one layer from the last, that layer gives you the embedding.

So today, for example, a lot of embedding models, if you guys are familiar with Hugging Face, you can download models from Hugging Face, right? So there are separate tabs for embedding models. In fact, there is also a leaderboard for embeddings. So look at there, you see things like Snowflake, one of the leading embedding models these days, or even from Alibaba the Qwen model. So you can just like you download LLM models, Llama or other open source models, right, there are open source embedding models available. So you can download one of the latest embedding models for your tasks.

So just as one example, there's one called MTEB, Massive Text Embedding Benchmark. Yeah, you can see, you know, I see here now, this is one I think. These are very popular. 4096 means that your embedding vector is of dimension 4096, which is very large. You don't need that if you want something lighter for just basic embedding, maybe less than a thousand dimensions. So you can choose the embedding model that suits your needs. So the takeaway is you can choose from there.

So you can look for something which is like less than a thousand dimensions. You don't want very high dimensionality. So if you use higher dimensional embeddings when your problem does not require it, your computational cost will become higher. And this increases the memory requirements constantly.

Okay, now I'm talking about data leakage. Okay. So this is very important. Why is it important? Because, as I said, when you train a model, right, it is important that you split the data into training, validation, and test, right? And it's important that the validation and test data should not have any information or any data which is part of the training set. And so it's possible that, you know, sometimes without knowing, that happens, and you wouldn't know. It's happened to me many times that, you know, we did this, but at the end of the day, you see the model is doing very good, you don't expect it to do that well, and when you test it in production, the performance is going down. That is partly because there is data leakage.

Now, data leakage can happen for various reasons. One is because a feature is sort of an aggregate of another feature or another variable, and that can also create a temporal connection. An example: suppose you have a model, right, which you want to predict something. But then in that model you have a feature, and you also have, let's say, a yearly salary. So when you calculate, what happens with the salary? So you create your data and you have this yearly salary, right? Then you, at some point, use that yearly salary for predicting. Then you also have monthly salary. So yearly salary is calculated as an aggregation of monthly salary. Then if you use monthly salary as your input variable, that can leak some information about the yearly salary, right? So that is a form of data leakage.

Sometimes what happens is there could be duplicate data, and without knowing, when you do the training and test split, some part of the duplicate data, one copy will come into the training data set and the other into the test data. That's another cause.

The other cause is because the data has a time dependence. So that is similar to what happens in the case of interpolation, right? Because when you do the imputation, you are creating a dependency between that value and the previous values. So in time series, you know that when you have time series data, you should never do random splitting. You should always split time series models based on time. You use data up to some time for training, then from that time to some window for validation, then from that point to a later time for testing. So you should always do this because time series data does not adhere to the identically and independently distributed assumption. There's always a dependency between observations up to some time lag. So if you split time series data randomly, you can create data leakage. Because the samples are not independent.

So these are the three or four reasons for data leakage. This is important because if you don't address this, you may see good performance of your model during development, but at the end of the day when you test the model in real life, you see that it is not actually doing well. It was doing well during development because of the data leakage, not because of the model's true performance. This is very important.

So this is the process. We saw earlier, you should split the data before imputation, you know, train, test, validation, then do the imputation. Don't impute before splitting. Okay, so first you remove duplicates, then split. Don't do the other way. Same for another reason: when you create a Box Cox transformation, that again creates a dependency. So you should not do the Box Cox transformation and then split into train and test. You should split into train and test first, then do the Box Cox transformation within each split separately. Same with standard scaling and normalization.

How to detect data leakage: you can measure the correlation between each feature and the target. If some features are highly correlated with the target, then you need to suspect there might be leakage. Maybe not always, but when you see unusually high correlations in the data, be suspicious.

And the temporal correlation between training and test data, the temporal correlation is measured using a function called autocorrelation. Basically, you take a value at time T and the same variable at time T plus some lag, right? So you compute the correlation between X at T and X at T plus tau. If you see that there's a high autocorrelation, then you should identify a time scale within which the correlation exists. So that's how you measure temporal correlation.

Okay, so the last topic we are going to cover today is feature selection. Now that you have engineered a lot of features, right, by doing embedding, or doing standard feature engineering techniques, and other things like that, generally people say more features are good because more signals are good, right. Machine learning is about combining a lot of signals. But then the problem is that too many features are also not good, because you need enough data to support that. So if you have more features than data, right, it can cause overfitting. It comes from the simple understanding of linear algebra, right? When you do matrix decomposition, right, when the number of columns is more than the number of rows, right, you do not get a good solution. So number of features compared with the number of observations can lead to overfitting if you don't have enough samples to fit the model. Okay. So that is one reason.

And the features need to be stored in memory, right? So if you use too many features, it may require more computational resources like memory. And more features also means that when you send one sample for inference, right, it has to fetch all the features and compute using all the features. It can increase the latency. And more features also require more data. So you want to keep an optimum number of features for your model. Even though you may be able to create a large number of features, you want to keep the optimal number of features for your model. So you use some method for feature selection. You select the best features. You should not keep all the features. Keep the best features.

So how do you do feature selection? The two things you are concerned with when doing this: first, is that feature important to the model? For example, when I add that feature to the model, is it increasing its accuracy or whatever performance measure you want, right? I build that model without the feature, I build that model with the feature. Is the difference substantial? Then I need to keep the feature. Okay, during development. The other thing is, is that feature helping me to generalize better? Meaning that when I develop this model, but then I am going to use it in production, it is going to make inference on new data, unseen data, right? In the case of unseen data, the distribution is going to be different, right? We may start seeing data which is not part of the training data, right? So you want the model to also generalize well, to perform well on unseen data, right?

So what happens is that features may be doing good as part of your training and validation sets. But when you start using it for the test set, right, unseen data, it may not do that well. So keep this in mind. It's not just enough that a feature makes the development model better. You need to make sure that the features also help generalize.

So how do you improve this? How do you select features? What are the various ways of feature selection? You can do some kind of iterative method. So you systematically, right, you start with one feature, right? Then you add another feature and see, you know, is it improving the accuracy? So let's say you have ten features. You start with the model with one feature. Then from the remaining nine features, right, you add them one by one and see which one is increasing the accuracy. So then you have two features. Then you have eight features remaining, again, you do the same thing. So you do this recursively. Okay. So you can do either forward selection, where you start with one and add features, or you start with all the features and remove features one by one. So that's the standard way of doing it.

There's also feature interaction. That's another aspect. So if you include feature interactions, how do you handle that? You could have a very large number of combinations, right? If you have N features, you have N factorial combinations. Yeah, so that's combinatorially explosive. But in practice, you find from domain experience, that's why a lot of domain knowledge is important. You know that certain features don't contribute or don't combine to make meaningful interactions. Technically, you could create all interactions and then select, but also, you have models, and you tend to follow which variables matter based on your previous testing. Yeah, previous testing can inform current feature selection.

For example, if there's a model in production and model A has performed better than model B for certain features, looking at that kind of information you can make decisions. But generally, what happens is that first order features, when you do the interaction terms, their importance would be less than the individual features. And third order interactions will be even less important. Okay. So normally they don't contribute much after the second order interactions. But yes, if there are a lot of features, you need to try various combinations.

So there is this idea called Shapley values. The simple way of doing feature selection is either you build recursively, either include more features starting with one feature, or you start with a full set of features and remove one by one. That's one approach. And so there's an entropy based method where you compute the entropy difference between the feature included and feature not included, right, in the model. And that also can be used. Then you rank the features based on which one contributes more. Then you can select features based on that. That's the entropy based approach.

But what people commonly use is called Shapley values. So it is a concept borrowed from game theory. This method was invented by a person named Lloyd Shapley. So what they do is think of each prediction as a cooperative game driven by all the features to make a prediction. Okay. So then you find out the contribution of each feature into your prediction. So when it predicts, right, take a particular record and make an inference with the model, right? So when you make an inference, let's say you are predicting this particular transaction as, let's say, fraud. Now, in that prediction, each feature adds some contribution. So this method calculates what is the contribution of each feature. So based on that contribution, you can decide which features to keep.

So for example, for this kind of a problem. Okay, so there are four features here. So this is age and some other features. So you can do this at two levels. One is at the global level, the model level. So you want to see, when you build a model, which features are important for that model. So that is at the model level, when you develop a model, you want to do feature selection. The other is that now you have the model, right, in production. For example, you build a credit scoring model. Okay. And then you did this Shapley values analysis and identified the best features. Okay, that's very good. Now that model is in production and you are scoring each customer, right, for a loan application, right, using that model. Now, when you make a decision, right, whether the credit score is high or low, you can look at the Shapley values for that particular inference, that particular transaction, and decide what contributed to a loan rejection. So there are two ways of using Shapley values.

So to see this, for example, right, what this is telling here is that when you build this model and compute the Shapley values, let's say this is a classification problem. Okay. Now, each feature, these are the features, it is showing the importance, right? So the colour indicates whether the importance is low or high. So one colour means the importance is low and another colour means the importance is high. And here it shows minus and plus. So this means that this feature has a very high contribution towards the negative class. It is supporting the negative class. Okay. Whereas another feature is very important for the positive class. Similarly, another feature is very low for the negative class, but a little high, not very high, for the positive class. Okay? So it gives these actual numerical values.

Then we can sort the features based on those values, right? You sort the features by their Shapley values, you see which feature has the highest importance, right? So for this model, the top feature has the highest importance, and it contributes mainly to, say, the negative class. Okay. The next feature is important for the positive class, and so on. So you probably want to keep only the features above a certain cutoff. You sort the features based on the Shapley values and you can decide a cutoff. Then any features below that cutoff, you can drop. You keep the features above the cutoff for your model. So that is one use.

Now when you make a single prediction, let's say I took this model into production, I scored a customer, and for that particular record I saw the Shapley value for one feature is 4.98 and the Shapley value for another is minus 2. So these two features contributed to the rejection of that loan application. Then I know what caused the rejection. So Shapley values can be useful both at the global level for feature selection and at the individual prediction level for feature importance. That's what I wanted to say.

Yeah, so this notebook has everything explained. How the Shapley values are computed, and there's some papers, right? You can look at those. So you see the Shapley values for this example. For example, here, right, for credit scoring, the most important feature is the difference between the first payment date and the first due date. Okay, so that's, you know, the thing, right? Your due date for the credit card, then when you actually paid that month's credit card bill, right? They look at the difference. That's the most important feature. So that tells you that you should pay your credit card bills on time. Okay. Then there's some other features like GPS latitude, longitude, state, account creation date, due day of the week, age. So for each of these features, we can look at the Shapley values and see whether it's positive or negative and how much each contributes.

So what I suggest is that all of you access this notebook. Access the link, then don't work on it directly, but create a copy for yourself. Okay, create a copy for yourself. So save a copy and just run it. It's free, you can connect to the runtime. So this is going to be an exercise for you. This is easy to do, just run the notebook. Okay. So if you want to understand more about Shapley values, please run this notebook. Okay.

So basically, to summarize the best practices: split the data first, divide your data by splitting randomly. If you oversample your data, oversample after splitting. Scale your data after splitting. Use statistics from the training data to scale your features. Like your Box Cox transformation, do it after splitting. Understand how your data is distributed and processed. Keep a lineage of your data. It's very important because sometimes you want to go back and check, you know, why the model failed, because you have trained on some transformations, right? So you need to keep track of data versions. Data versioning is as important as model versioning. And use the features that work well. If your features are not used in your model, right, remove them. Because you don't want them, they might get out of distribution.

So, any questions? Yeah. In the feature selection, can you do PCA? PCA is a type of dimensionality reduction. So if you have more or less nonlinear dependency, then PCA is not the best choice. So PCA assumes linear relationships and PCA features are not always the most interpretable. So I would think of PCA as a separate approach. So what you can do is you can do PCA first and then do feature selection on top of that.